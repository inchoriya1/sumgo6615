# Claude Code 로컬 설정 — 내 컴퓨터 전용 (절대경로가 들어 있음)
.claude/settings.local.json

# 운영체제가 만드는 파일
.DS_Store
Thumbs.db
desktop.ini

# ── 개인정보가 든 실무 자료 ──
# 급여대장·근태기록에는 주민등록번호와 급여액이 들어 있습니다.
# 이 저장소는 공개 배포되므로 절대 커밋하지 않습니다.
엑셀자료/

# 암호를 걸어 내보내는 페이지의 '원본'.
# 여기에는 실명이 그대로 들어 있습니다. 커밋되는 것은
# tools/build-locked-page.py 로 암호화한 excel-4.html 뿐입니다.
*.src.html

# 파이썬이 만드는 캐시
__pycache__/
*.pyc

# 오빠두 마스터 클래스 원본 예제 폴더 · 4-07 실습 파일(25MB 등) · 장별 zip 은
# 2026-09-18 부터 저장소에 넣습니다. 가장 큰 파일이 24.8MB 라
# GitHub 한도(50MB 경고 · 100MB 거부) 안쪽입니다. 다시 막지 마세요.
