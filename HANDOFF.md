# 인수인계 메모 — 2026-09-02

새 세션에서 이 작업을 이어받을 때 **이 파일과 `README.md`를 먼저 읽으세요.**

## 이 프로젝트가 무엇인가

`sumgo` — 컴퓨터를 거의 다뤄보지 않은 분(주로 50대 이상)을 위한
컴퓨터 기초 강의 사이트입니다. 빌드 도구 없이 HTML·CSS·JS 파일만으로 되어 있고,
파일을 그냥 열어도 동작합니다.

| 파일 | 내용 |
|---|---|
| `index.html` | 첫 화면 |
| `check.html` `check.js` | **실력 진단.** 답하면 먼저 볼 곳을 알려줍니다 |
| `excel.html` `ppt.html` `hangul.html` `word.html` `ai.html` | 1~5과목 **안내 화면** |
| `excel-1~3` `ppt-1~3` `hangul-1~3` `word-1~3` | 강의안 12개 |
| `ai-1~5` | AI 강의안 5개 |
| `style.css` `script.js` | 모든 페이지가 함께 씀 |

지켜야 할 규칙 8가지는 `README.md`에 정리되어 있습니다.
**고치기 전에 반드시 읽으세요.** 특히 `00 — 준비` 장은 이제
**강의안 열일곱 곳**을 함께 고쳐야 합니다.
(AI 과목만 '손동작' 이 아니라 '마음가짐' 세 가지입니다.)

## 이번에 한 일 — 네 과목을 모두 부로 나눴습니다

사용자 요청: **과목마다 파트를 나누고 내용을 더 상세하게.**
파트는 **별도 페이지**로 하기로 정했습니다.
엑셀을 먼저 만들어 틀을 잡고, 같은 틀로 나머지 세 과목을 이어서 했습니다.

원래 과목당 9~10장이던 것이 **과목당 27~31장**이 되었습니다.
원본 네 파일(`excel/ppt/hangul/word.html`)은 **과목 안내 화면으로 덮어썼습니다.**
(백업은 세션 임시 폴더에만 있습니다. git 이력이 없으니 주의.)

### 과목별 구성

각 과목 초보자 과정을 조사해서(엑셀웍스·오빠두엑셀·경기도 GSEEK·
예스폼·마이크로소프트 공식 교육) 공통으로 다루는 순서를 뽑아 세 부로 끊었습니다.

| 과목 | 1부 | 2부 (핵심) | 3부 |
|---|---|---|---|
| **엑셀** | 표 만들기·고치기·행열·저장 (100분) | **계산 시키기** — 수식·함수 다섯·채우기 핸들·절대참조·IF (110분) | 서식·조건부 서식·정렬·필터·틀 고정·차트·인쇄 (110분) |
| **PPT** | 장 만들기·레이아웃·글자 (90분) | **보여줄 것 넣기** — 사진·자르기·도형·**줄 맞추기**·표·스마트아트 (100분) | 테마·마스터·쪽 번호·전환·발표자 도구·유인물 (90분) |
| **한글** | 글쓰기·글자 모양·문단 모양 (90분) | **표와 그림** — 표 꾸미기·셀 합치기·본문과의 배치 (100분) | 편집 용지·쪽 번호·머리말·스타일·문서마당·인쇄 (90분) |
| **워드** | 문서 쓰기·글자와 문단 (80분) | **스타일** — 제목 1/2·스타일 고치기·목록·표·사진 (90분) | **자동 목차**·쪽 번호·구역·찾아 바꾸기·메모와 고친 자국·인쇄 (90분) |

**과목마다 2부가 핵심입니다.** 고칠 때 이 축을 흐리지 마세요.
`README.md`의 "과목을 부로 나누는 방식" 절에 적어 두었습니다.

### 집필하며 정한 원칙

- **1부에서는 꾸미지도, 계산하지도 않습니다.** 손이 익기 전에
  수식이나 색부터 하면 따라오지 못합니다. 각 과목 1부 01장에 그 이유를 적어 두었습니다.
- **각 부에 "가장 큰 사고"를 하나씩** `.note stop`으로 박아 두었습니다.
  엑셀 3부 **"한 열만 정렬하면 표가 어긋난다"**,
  PPT 2부 **"크기는 모서리를 잡아라"**,
  한글 2부 **"그림은 문서에 포함에 체크하라"**,
  워드 3부 **"인쇄 전에 목차를 업데이트하라"**.
- **어려운 장은 "건너뛰어도 된다"고 명시**했습니다.
  엑셀 2부 IF, PPT 2부 스마트아트, 한글 3부 스타일, 워드 3부 구역 나누기.
- **한글은 무료 대안이 없다**는 기존 판단을 그대로 유지했습니다.
  1부 03장과 과목 안내 화면 양쪽에서 **워드로 건너뛰라**고 안내합니다.

## 곁들여 고친 것 — 화면 공간을 안 쓰던 문제

"공간이 많은데 줄바꿈을 너무 일찍 한다"는 지적을 받고 고쳤습니다.
원인이 **셋**이었습니다.

**첫째 — 폭을 `ch` 로 재고 있었습니다.**
`style.css`가 본문 폭을 **`58ch`** 로 잡고 있었는데, `ch` 는 **숫자 `0` 하나의 폭**입니다.
한글 한 글자는 그보다 **1.86배** 넓어서, `58ch` 는 영문으로는 57자지만
**한글로는 31자**밖에 안 됩니다. `main` 은 820px인데 본문은 563px에서 끊겨
오른쪽에 **257px이 늘 비어** 있었습니다.

`ch` 는 이제 프로젝트 어디에도 없습니다.

**둘째 — 바깥 폭 자체가 좁았습니다.** 첫째를 고친 뒤에도
"양쪽 여백이 너무 많다"는 지적이 이어졌습니다. 재보니 1920px 화면에서
`.shell` 이 1180px에 묶여 **양쪽에 725px, 화면의 38%가 비어** 있었습니다.

**셋째 — `em`도 요소마다 달랐습니다.** `em`은 *그 요소의* 글자 크기를 따라가서,
작은 글씨일수록 상자가 좁아집니다. 그래서 첫 화면에서 오른쪽 끝이
**여섯 군데**(614·634·660·729·792·1384)에서 제각각 멈추고 있었습니다.
`calc(var(--base) * 글자수)`로 못박아 한 줄로 맞췄습니다.

**넷째 — 글자 크기를 바꾸면 여백이 흔들렸습니다.**
`--measure`를 `--base`에 연동해 두니 `보통 / 크게 / 아주 크게`를 누를 때마다
글 상자가 늘었다 줄었다 했습니다. `936px`로 **못박았습니다**(규칙 1의 예외).
이제 글자 크기를 바꿔도 **여백은 325px 그대로**이고 글자만 커집니다.

### 최종 값

| | 처음 | 지금 |
|---|---|---|
| `.shell` | 1180px | **1580px** |
| `--content` (`main`·표·카드·구분선) | 820px | **1180px** |
| `--rail` | 236px | **290px** |
| `--measure` (본문 한 줄) | `58ch` (한글 31자) | **`var(--content)`** = 1180px |

넷의 합이 맞물립니다 — `290 + 54 + 1180 + 56 = 1580`.

### 결과

| 화면 폭 | 좌우 여백 (처음 → 지금) |
|---|---|
| 1920 | 725px (38%) → **325px (17%)** |
| 1440 | 260px → **0** |
| 1280 | 100px → **0** |

여백은 **글자 크기 세 단계에서 모두 325px로 동일**합니다.

**다섯째 — 본문이 칸을 안 채우고 있었습니다.** `--measure`가 936px이라
1180px 칸 안에서 오른쪽에 **244px 빈 띠**가 늘 남고, 거기서 줄이 바뀌었습니다.
줄 안쪽 채움은 재보니 **평균 96%**로 멀쩡했고(`keep-all` 문제 아님),
순전히 상자가 칸보다 좁아서 생긴 일이었습니다.
`--measure: var(--content)`로 바꿔 **글이 칸을 꽉 채우게** 했습니다.

**여섯째 — 차례 칸이 없는 화면은 오른쪽 344px 이 비어 있었습니다.**
`--content: 1180px` 는 `차례 칸 290 + 사이 54` 를 뺀 값입니다.
그런데 첫 화면·과목 안내에는 **차례 칸이 없어서** 그 자리를 아무것도 안 채웁니다.
`section` 은 1524px 인데 그 안의 `.guide` · `.band` · `.subjects` 가
1180px 에서 끊기고 있었습니다.

```css
.shell:not(.with-rail) { --content: 100%; --measure: 100%; }
```

`--measure` 도 **같이** 적어야 합니다. `:root` 의 `--measure: var(--content)` 는
`:root` 에서 이미 1180px 로 계산돼 내려오므로 `--content` 만 바꿔서는 안 따라옵니다.
(한 번 빠뜨려서 `.guide` 만 1180px 로 남는 걸 확인하고 고쳤습니다.)

강의안 머리말의 `.lede` 도 같은 이유로 1180px 에서 멈춰 있었습니다.
머리말은 차례 칸 위쪽 전체 폭을 쓰므로 `max-width: none` 으로 풀었습니다.
큰 제목 `h1` 도 마찬가지입니다.

이제 **오른쪽 끝이 한 지점**입니다 — 머리말·글·표·카드·구분선이 모두 같은 자리에서 끝납니다.

### 목록을 세로로 바꿨습니다

`지금 배울 수 있는 것`(첫 화면)과 `차례`(과목 안내)의 카드를
가로 4열 → **세로 한 줄에 하나씩**으로 바꿨습니다.
가로로 늘어놓으니 칸이 262px까지 좁아져 설명이 예닐곱 줄로 접혔습니다.

카드 안은 두 칸입니다 — 왼쪽에 이름표와 제목, 오른쪽에 설명과 링크.
좁은 화면(560px 이하)과 인쇄에서는 위아래로 풀립니다.

### 글자 크기 단추가 안 먹던 곳 12군데

`px`로 박혀 있어서 `크게 / 아주 크게`를 눌러도 **안 커지던 글씨**를 찾아 고쳤습니다.
대상이 50대 이상인데 정작 차례와 안내 상자 제목이 15px에 묶여 있었습니다.

`.eyebrow` · `.facts` · `.back` · `.rail-title` · **`.toc a`(차례)** ·
`.prefs-title` · `.theme-btn` · `.sec-tag`(장 번호) · `.steps` 번호 동그라미 ·
**`.note-label`(안내 상자 제목)** · `.say-label` · `.duo-label`

이제 차례 글자가 15.1 → 17.2 → 19.3px로 함께 커집니다.
(`.size-set button`의 13/16/19px는 `보통/크게/아주 크게`를 미리 보여주는
일부러 다른 크기라 그대로 두었습니다.)

### 시행착오 기록 (다시 하지 마세요)

중간에 **차례 칸이 없는 화면(첫 화면·과목 안내)만 좁히는** 규칙
(`.shell:not(.with-rail)`)을 넣었다가 **되돌렸습니다.**
안내 상자와 카드줄의 단차는 줄었지만 **좌우 여백이 오히려 늘어서**
사용자가 원한 방향과 반대였습니다.
사용자가 말한 "여백이 부자연스럽다"는 **여백이 너무 많다**는 뜻이었습니다.

## 새로 만든 것 — 실력 진단과 AI 다섯 부

### 실력 진단 `check.html`

문제를 푸는 시험이 아닙니다. **"이건 할 수 있다 / 이건 모르겠다"** 에
5단계(전혀 모름·못함·보통·잘함·매우 잘함)로 표시하는 자기 점검입니다.

- **35개 문항**, 6개 묶음(컴퓨터 기본 · 엑셀 · PPT · 한글 · 워드 · AI)
- 결과: 과목별 막대와 수준, 그리고 **먼저 볼 부** 목록
- **1점 이하로 답한 문항이 가리키는 부**를 뽑되, 많이 걸린 곳부터 **세 곳만**
  보여줍니다(`check.js` 의 `TOP_N`). 나머지는 아래 한 줄로 적고, 어떤 문항이었는지는
  `어떤 것인지 보기` 로 접어 둡니다
- 저장: **종이(PDF, 인쇄)** 와 **사진(PNG)**
- 답은 `localStorage` 에 남아 창을 닫아도 유지됩니다

**PNG는 바깥 라이브러리 없이 `canvas` 에 직접 그립니다.**
`html2canvas` 같은 것을 쓰면 인터넷 없이는 안 되기 때문입니다.
화면 색 변수를 읽어 쓰므로 어두운 화면에서는 어둡게 나옵니다.

**문항을 늘릴 때 주의** — `data-part` 값이 `check.js` 의 `PARTS` 표에 없으면
결과에서 **조용히 빠집니다.** 새 부를 만들면 그 표에도 넣으세요.

### AI 과목을 다섯 부로

1~4과목은 *배우는 순서*로 나눴지만, AI는 **맡기는 일의 종류**로 나눴습니다.
사용자가 "엑셀파트/ppt파트/한글파트/워드파트 등"으로 나눠 달라고 한 것을 따랐습니다.

| 부 | 내용 | 소요 |
|---|---|---|
| 1부 | **시작하기** — 셋은 같은 물건, 네 조각, 다섯 마디, 되돌리기 | 90분 |
| 2부 | **파일과 폴더** — 바탕 화면 정리, 이름 바꾸기, 찾기, 중복 | 90분 |
| 3부 | **엑셀** — 표 만들기, 정리, 묶어서 합계, 합치기, 엑셀 심술 | 100분 |
| 4부 | **한글·워드** — 요약, 초안, 다듬기, 말투 바꾸기, PDF | 100분 |
| 5부 | **파워포인트** — 뼈대, 장별 내용, 옮기기, 대본과 예상 질문 | 90분 |

원래 13장이던 것이 **46장**이 되었습니다.
**1부만 필수**이고 나머지는 골라 볼 수 있게 했습니다.

집필하며 정한 것:

- 부마다 끝에 **"그대로 베껴 쓰는 말"** 모음 장을 두었습니다.
  필요할 때 그 장만 열어 복사하면 됩니다.
- 부마다 `.note stop` 경고를 하나씩 — 2부 "지워 줘라고 하지 마라",
  3부 "숫자는 반드시 확인", 4부 "받은 글을 그대로 내보내지 마라",
  5부 "내가 말할 수 있는 것만 넣어라".
- **앞의 네 과목과 계속 이어 놓았습니다.** 3부는 1과목 2부의 "빈 칸과 0은 다르다"를,
  5부는 2과목 1부의 "한 장에 한 가지"를 그대로 가리킵니다.

## 남은 일

- [ ] **전체 검토받기** — 네 과목 12개 강의안의 분량·깊이·말투.
      아직 사용자 확인을 못 받았습니다.
- [ ] **줄 길이 확인** — 본문이 칸을 꽉 채웁니다.
      강의안(1180px)은 보통 **65자**, 첫 화면·과목 안내(1524px)는 **84자**입니다.
      한글 통상 범위(35~45자)를 꽤 넘습니다.
      **사용자가 "여백 없이 꽉 채워 달라"고 세 번 요청해서 이렇게 한 것입니다.**
      읽기 어렵다고 하시면 `--measure`에 `1000px` 정도를 주세요.
      다만 그러면 글 오른쪽에 빈 띠가 다시 생기고, 오른쪽 끝 정렬도 깨집니다.
      **둘 중 하나는 포기해야 합니다.**
- [ ] **시간 배분 재검토** — 부당 80~110분으로 잡았으나
      실제로 한 번 진행해 보고 정할 일입니다.
- [ ] **git 저장소로 만들기** — 아직 저장소가 아닙니다. `git init`부터 필요합니다.
      **원본 네 파일을 덮어썼으므로 되돌릴 수단이 없는 상태입니다.** 우선순위가 높습니다.
- [ ] **Vercel 배포** — 아직 올리지 않았습니다.
- [ ] **표의 가로 스크롤 표시** — 좁은 화면에서 표와 그림이
      `.tbl-wrap`(`overflow-x:auto`) 안에서 잘립니다. 밀면 보이기는 하는데
      **밀 수 있다는 표시가 없습니다.** 사용자가 "모바일은 안 본다"고 하여 손대지 않았습니다.
- [ ] **복사 단추 실제 확인** — `.say-copy` 를 브라우저 자동화로 눌렀더니
      "복사가 안 됩니다"가 떴습니다. **사람이 직접 누른 것이 아니라
      클립보드 권한이 막힌 것**으로 보입니다(이전 세션에서는 실제 클릭으로 동작 확인).
      한 번 손으로 눌러서 확인해 주세요.
- [ ] **진단 결과 PNG 실제 확인** — 코드 경로와 예외 처리는 확인했지만
      내려받은 그림을 눈으로 보지는 못했습니다(샌드박스가 내려받기를 막습니다).

## 확인해 둔 것

- **모든 페이지의 내부 링크·앵커 검사 통과** (18개 파일, 깨진 것 0개).
- 강의안 12개 전부 **장 수 = 목차 항목 수**, `00 — 준비` 장 있음,
  `03장` 실습에 무료 대안 안내 있음, `data-subject` 올바름.
- 화면 폭 1905·1425·1265·885와 글자 크기 세 단계에서
  **가로 넘침 0개**, 콘솔 오류 없음. 밝은 화면·어두운 화면 모두.
- 첫 화면·과목 안내 네 곳, 강의안 12곳, `ai.html` 까지 전부 확인했습니다.
- 확인 목록 저장이 **부마다 분리**되는 것을 클릭으로 확인했습니다
  (`sumgo-check:excel-1.html:2` 처럼 파일 이름이 키에 들어갑니다).
- `.say`(지시문 카드)는 이제 **`ai.html`에서만** 쓰입니다.
  `README.md`의 `.say` 설명도 실제 동작에 맞게 고쳤습니다
  (단추는 마크업에 직접 넣어야 하고 자동으로 생기지 않습니다).

## 이 세션에서 쓴 것

로컬 확인용으로 `.claude/launch.json` 을 만들었습니다
(`python -m http.server 8123`). `file://` 로 열면 CSS가 안 붙어서
스타일 확인이 안 됩니다. 반드시 서버로 띄워서 보세요.
