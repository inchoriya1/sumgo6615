# 인수인계 메모 — 2026-09-02 (마지막 손질 2026-09-16)

새 세션에서 이 작업을 이어받을 때 **이 파일과 `README.md`를 먼저 읽으세요.**
절은 **일한 순서대로** 쌓여 있습니다. 맨 뒤 세 절
(`남은 일` · `확인해 둔 것` · `이 세션에서 쓴 것`)이 지금 상태입니다.

## 이 프로젝트가 무엇인가

`sumgo` — 컴퓨터를 거의 다뤄보지 않은 분(주로 50대 이상)을 위한
컴퓨터 기초 강의 사이트입니다. 빌드 도구 없이 HTML·CSS·JS 파일만으로 되어 있고,
파일을 그냥 열어도 동작합니다.

| 파일 | 내용 |
|---|---|
| `index.html` | 첫 화면 |
| `check.html` `check.js` | **실력 진단.** 답하면 먼저 볼 곳을 알려줍니다 |
| `excel.html` `ppt.html` `hangul.html` `word.html` `ai.html` | 1~5과목 **안내 화면** |
| `excel-1~3` `ppt-1~3` `hangul-1~3` `word-1~3` | 강의안 12개 |
| `ai-1~5` | AI 강의안 5개 |
| `excel-4` `excel-5` | **엑셀 실무 편** — 우리 회사 급여대장. 암호가 걸린 사내용 |
| `excel-m1~m9` | **엑셀 마스터 클래스** — 여덟 장과 부록. 공개 |
| `강의예제/` | 마스터 클래스 실습 파일 · 복습 · 종합문제 |
| `style.css` `script.js` | 모든 페이지가 함께 씀 |

지켜야 할 규칙 8가지는 `README.md`에 정리되어 있습니다.
**고치기 전에 반드시 읽으세요.** 특히 `00 — 준비` 장은 이제
**강의안 스물여덟 곳**을 함께 고쳐야 합니다.
(실무 편 둘은 `*.src.html` 을 고치고 다시 잠가야 합니다.)
(AI 과목만 '손동작' 이 아니라 '마음가짐' 세 가지입니다.)

## 이번에 한 일 — 네 과목을 모두 부로 나눴습니다

사용자 요청: **과목마다 파트를 나누고 내용을 더 상세하게.**
파트는 **별도 페이지**로 하기로 정했습니다.
엑셀을 먼저 만들어 틀을 잡고, 같은 틀로 나머지 세 과목을 이어서 했습니다.

원래 과목당 9~10장이던 것이 **과목당 27~31장**이 되었습니다.
원본 네 파일(`excel/ppt/hangul/word.html`)은 **과목 안내 화면으로 덮어썼습니다.**
(백업은 세션 임시 폴더에만 있습니다. git 이력이 없으니 주의.)

### 과목별 구성

각 과목 초보자 과정을 조사해서(엑셀웍스·오빠두엑셀·경기도 GSEEK·
예스폼·마이크로소프트 공식 교육) 공통으로 다루는 순서를 뽑아 세 부로 끊었습니다.

| 과목 | 1부 | 2부 (핵심) | 3부 |
|---|---|---|---|
| **엑셀** | 표 만들기·고치기·행열·저장 (100분) | **계산 시키기** — 수식·함수 다섯·채우기 핸들·절대참조·IF (110분) | 서식·조건부 서식·정렬·필터·틀 고정·차트·인쇄 (130분) |
| **PPT** | 장 만들기·레이아웃·글자 (90분) | **보여줄 것 넣기** — 사진·자르기·도형·**줄 맞추기**·표·스마트아트 (100분) | 테마·마스터·쪽 번호·전환·발표자 도구·유인물 (90분) |
| **한글** | 글쓰기·글자 모양·문단 모양 (90분) | **표와 그림** — 표 꾸미기·셀 합치기·본문과의 배치 (100분) | 편집 용지·쪽 번호·머리말·스타일·문서마당·인쇄 (100분) |
| **워드** | 문서 쓰기·글자와 문단 (80분) | **스타일** — 제목 1/2·스타일 고치기·목록·표·사진 (90분) | **자동 목차**·쪽 번호·구역·찾아 바꾸기·메모와 고친 자국·인쇄 (100분) |

**과목마다 2부가 핵심입니다.** 고칠 때 이 축을 흐리지 마세요.
`README.md`의 "과목을 부로 나누는 방식" 절에 적어 두었습니다.

### 집필하며 정한 원칙

- **1부에서는 꾸미지도, 계산하지도 않습니다.** 손이 익기 전에
  수식이나 색부터 하면 따라오지 못합니다. 각 과목 1부 01장에 그 이유를 적어 두었습니다.
- **각 부에 "가장 큰 사고"를 하나씩** `.note stop`으로 박아 두었습니다.
  엑셀 3부 **"한 열만 정렬하면 표가 어긋난다"**,
  PPT 2부 **"크기는 모서리를 잡아라"**,
  한글 2부 **"그림은 문서에 포함에 체크하라"**,
  워드 3부 **"인쇄 전에 목차를 업데이트하라"**.
- **어려운 장은 "건너뛰어도 된다"고 명시**했습니다.
  엑셀 2부 IF, PPT 2부 스마트아트, 한글 3부 스타일, 워드 3부 구역 나누기.
- **한글은 무료 대안이 없다**는 기존 판단을 그대로 유지했습니다.
  1부 03장과 과목 안내 화면 양쪽에서 **워드로 건너뛰라**고 안내합니다.

## 곁들여 고친 것 — 화면 공간을 안 쓰던 문제

"공간이 많은데 줄바꿈을 너무 일찍 한다"는 지적을 받고 고쳤습니다.
원인이 **셋**이었습니다.

**첫째 — 폭을 `ch` 로 재고 있었습니다.**
`style.css`가 본문 폭을 **`58ch`** 로 잡고 있었는데, `ch` 는 **숫자 `0` 하나의 폭**입니다.
한글 한 글자는 그보다 **1.86배** 넓어서, `58ch` 는 영문으로는 57자지만
**한글로는 31자**밖에 안 됩니다. `main` 은 820px인데 본문은 563px에서 끊겨
오른쪽에 **257px이 늘 비어** 있었습니다.

`ch` 는 이제 프로젝트 어디에도 없습니다.

**둘째 — 바깥 폭 자체가 좁았습니다.** 첫째를 고친 뒤에도
"양쪽 여백이 너무 많다"는 지적이 이어졌습니다. 재보니 1920px 화면에서
`.shell` 이 1180px에 묶여 **양쪽에 725px, 화면의 38%가 비어** 있었습니다.

**셋째 — `em`도 요소마다 달랐습니다.** `em`은 *그 요소의* 글자 크기를 따라가서,
작은 글씨일수록 상자가 좁아집니다. 그래서 첫 화면에서 오른쪽 끝이
**여섯 군데**(614·634·660·729·792·1384)에서 제각각 멈추고 있었습니다.
`calc(var(--base) * 글자수)`로 못박아 한 줄로 맞췄습니다.

**넷째 — 글자 크기를 바꾸면 여백이 흔들렸습니다.**
`--measure`를 `--base`에 연동해 두니 `보통 / 크게 / 아주 크게`를 누를 때마다
글 상자가 늘었다 줄었다 했습니다. `936px`로 **못박았습니다**(규칙 1의 예외).
이제 글자 크기를 바꿔도 **여백은 325px 그대로**이고 글자만 커집니다.

### 최종 값

| | 처음 | 지금 |
|---|---|---|
| `.shell` | 1180px | **1580px** |
| `--content` (`main`·표·카드·구분선) | 820px | **1180px** |
| `--rail` | 236px | **290px** |
| `--measure` (본문 한 줄) | `58ch` (한글 31자) | **`var(--content)`** = 1180px |

넷의 합이 맞물립니다 — `290 + 54 + 1180 + 56 = 1580`.

### 결과

| 화면 폭 | 좌우 여백 (처음 → 지금) |
|---|---|
| 1920 | 725px (38%) → **325px (17%)** |
| 1440 | 260px → **0** |
| 1280 | 100px → **0** |

여백은 **글자 크기 세 단계에서 모두 325px로 동일**합니다.

**다섯째 — 본문이 칸을 안 채우고 있었습니다.** `--measure`가 936px이라
1180px 칸 안에서 오른쪽에 **244px 빈 띠**가 늘 남고, 거기서 줄이 바뀌었습니다.
줄 안쪽 채움은 재보니 **평균 96%**로 멀쩡했고(`keep-all` 문제 아님),
순전히 상자가 칸보다 좁아서 생긴 일이었습니다.
`--measure: var(--content)`로 바꿔 **글이 칸을 꽉 채우게** 했습니다.

**여섯째 — 차례 칸이 없는 화면은 오른쪽 344px 이 비어 있었습니다.**
`--content: 1180px` 는 `차례 칸 290 + 사이 54` 를 뺀 값입니다.
그런데 첫 화면·과목 안내에는 **차례 칸이 없어서** 그 자리를 아무것도 안 채웁니다.
`section` 은 1524px 인데 그 안의 `.guide` · `.band` · `.subjects` 가
1180px 에서 끊기고 있었습니다.

```css
.shell:not(.with-rail) { --content: 100%; --measure: 100%; }
```

`--measure` 도 **같이** 적어야 합니다. `:root` 의 `--measure: var(--content)` 는
`:root` 에서 이미 1180px 로 계산돼 내려오므로 `--content` 만 바꿔서는 안 따라옵니다.
(한 번 빠뜨려서 `.guide` 만 1180px 로 남는 걸 확인하고 고쳤습니다.)

강의안 머리말의 `.lede` 도 같은 이유로 1180px 에서 멈춰 있었습니다.
머리말은 차례 칸 위쪽 전체 폭을 쓰므로 `max-width: none` 으로 풀었습니다.
큰 제목 `h1` 도 마찬가지입니다.

이제 **오른쪽 끝이 한 지점**입니다 — 머리말·글·표·카드·구분선이 모두 같은 자리에서 끝납니다.

### 목록을 세로로 바꿨습니다

`지금 배울 수 있는 것`(첫 화면)과 `차례`(과목 안내)의 카드를
가로 4열 → **세로 한 줄에 하나씩**으로 바꿨습니다.
가로로 늘어놓으니 칸이 262px까지 좁아져 설명이 예닐곱 줄로 접혔습니다.

카드 안은 두 칸입니다 — 왼쪽에 이름표와 제목, 오른쪽에 설명과 링크.
좁은 화면(560px 이하)과 인쇄에서는 위아래로 풀립니다.

### 글자 크기 단추가 안 먹던 곳 12군데

`px`로 박혀 있어서 `크게 / 아주 크게`를 눌러도 **안 커지던 글씨**를 찾아 고쳤습니다.
대상이 50대 이상인데 정작 차례와 안내 상자 제목이 15px에 묶여 있었습니다.

`.eyebrow` · `.facts` · `.back` · `.rail-title` · **`.toc a`(차례)** ·
`.prefs-title` · `.theme-btn` · `.sec-tag`(장 번호) · `.steps` 번호 동그라미 ·
**`.note-label`(안내 상자 제목)** · `.say-label` · `.duo-label`

이제 차례 글자가 15.1 → 17.2 → 19.3px로 함께 커집니다.
(`.size-set button`의 13/16/19px는 `보통/크게/아주 크게`를 미리 보여주는
일부러 다른 크기라 그대로 두었습니다.)

### 시행착오 기록 (다시 하지 마세요)

중간에 **차례 칸이 없는 화면(첫 화면·과목 안내)만 좁히는** 규칙
(`.shell:not(.with-rail)`)을 넣었다가 **되돌렸습니다.**
안내 상자와 카드줄의 단차는 줄었지만 **좌우 여백이 오히려 늘어서**
사용자가 원한 방향과 반대였습니다.
사용자가 말한 "여백이 부자연스럽다"는 **여백이 너무 많다**는 뜻이었습니다.

## 새로 만든 것 — 실력 진단과 AI 다섯 부

### 실력 진단 `check.html`

문제를 푸는 시험이 아닙니다. **"이건 할 수 있다 / 이건 모르겠다"** 에
5단계(전혀 모름·못함·보통·잘함·매우 잘함)로 표시하는 자기 점검입니다.

- **44개 문항**, 7개 묶음(컴퓨터 기본 · 엑셀 · 엑셀 마스터 · PPT · 한글 · 워드 · AI)
- **엑셀 마스터 클래스 9문항**(2026-09-18 추가) — 장마다 하나, `data-part="excel-m1"`…`"excel-m9"`.
  과목은 `excel-m` 으로 **따로 셉니다.** 기초 엑셀과 한 막대로 합치면, 처음 오신 분이
  마스터 문항에 「전혀 모름」을 누를 때 기초 엑셀 막대까지 내려가기 때문입니다.
  묶음 머리에 「처음이면 건너뛰세요」라고 적어 두었습니다. 답을 안 하면 막대에 「안 함」.
  `PARTS` 에서는 `excel-3` 바로 뒤에 두어, 못한 개수가 같으면 기초 엑셀이 먼저 나옵니다
- 결과: 과목별 막대와 수준, 그리고 **먼저 볼 부** 목록
- **1점 이하로 답한 문항이 가리키는 부**를 뽑되, 많이 걸린 곳부터 **세 곳만**
  보여줍니다(`check.js` 의 `TOP_N`). 나머지는 아래 한 줄로 적고, 어떤 문항이었는지는
  `어떤 것인지 보기` 로 접어 둡니다
- 저장: **종이(PDF, 인쇄)** 와 **사진(PNG)**
- 답은 `localStorage` 에 남아 창을 닫아도 유지됩니다

**PNG는 바깥 라이브러리 없이 `canvas` 에 직접 그립니다.**
`html2canvas` 같은 것을 쓰면 인터넷 없이는 안 되기 때문입니다.
화면 색 변수를 읽어 쓰므로 어두운 화면에서는 어둡게 나옵니다.

**문항을 늘릴 때 주의** — `data-part` 값이 `check.js` 의 `PARTS` 표에 없으면
결과에서 **조용히 빠집니다.** 새 부를 만들면 그 표에도 넣으세요.

### AI 과목을 다섯 부로

1~4과목은 *배우는 순서*로 나눴지만, AI는 **맡기는 일의 종류**로 나눴습니다.
사용자가 "엑셀파트/ppt파트/한글파트/워드파트 등"으로 나눠 달라고 한 것을 따랐습니다.

| 부 | 내용 | 소요 |
|---|---|---|
| 1부 | **시작하기** — 셋은 같은 물건, 네 조각, 다섯 마디, 되돌리기 | 90분 |
| 2부 | **파일과 폴더** — 바탕 화면 정리, 이름 바꾸기, 찾기, 중복 | 90분 |
| 3부 | **엑셀** — 표 만들기, 정리, 묶어서 합계, 합치기, 엑셀 심술 | 100분 |
| 4부 | **한글·워드** — 요약, 초안, 다듬기, 말투 바꾸기, PDF | 100분 |
| 5부 | **파워포인트** — 뼈대, 장별 내용, 옮기기, 대본과 예상 질문 | 90분 |

원래 13장이던 것이 **46장**이 되었습니다.
**1부만 필수**이고 나머지는 골라 볼 수 있게 했습니다.

집필하며 정한 것:

- 부마다 끝에 **"그대로 베껴 쓰는 말"** 모음 장을 두었습니다.
  필요할 때 그 장만 열어 복사하면 됩니다.
- 부마다 `.note stop` 경고를 하나씩 — 2부 "지워 줘라고 하지 마라",
  3부 "숫자는 반드시 확인", 4부 "받은 글을 그대로 내보내지 마라",
  5부 "내가 말할 수 있는 것만 넣어라".
- **앞의 네 과목과 계속 이어 놓았습니다.** 3부는 1과목 2부의 "빈 칸과 0은 다르다"를,
  5부는 2과목 1부의 "한 장에 한 가지"를 그대로 가리킵니다.

## 새로 만든 것 — 엑셀 실무 편 (`excel-4.html`)

**엑셀 강의 1일차**용 강의안입니다. 세 부와 성격이 다릅니다 —
앞의 셋은 *내가 만드는 표*, 이것은 **남이 만들어 둔 파일을 읽고 돌리는 법**입니다.
교재는 회사에서 실제로 쓰는 **급여대장 파일**(시트 18장)입니다.

### 장 구성 (14장, 120분)

`00 준비` → `01 이 파일이 하는 일` → `02 넣는 곳과 나오는 곳` →
**`03~05 매월 세 단계`** → `06 검산` → **`07~11 쓰인 기술`** →
`12 문제 해결` → `13 확인 목록`.

**실습을 기술보다 앞에 두었습니다.** 무엇을 하는 것인지 본 다음에야
수식이 읽히기 때문입니다.

07~11장에서 다루는 기술은 다섯입니다 — **키 만들기(`&`)** ·
**조건 붙은 합계(`SUMIF`/`SUMIFS`/`COUNTIFS`)** · **이름 정의 + `MATCH`/`INDEX`** ·
**`VLOOKUP` 의 TRUE/FALSE** · **"비었으면 기본값" 짜임과 `ROUNDDOWN`**.
이 파일에 실제로 쓰인 것이 그것뿐이라서 그렇게 잡았습니다.

### 실습 자료 — `엑셀자료/강의1일차_예제/`

**원본은 손대지 않았습니다.** 사본을 만들어 8월분만 비웠습니다.

| 파일 | 무엇 |
|---|---|
| `…_급여대장_연간관리_예제.xlsx` | 8월분을 비운 사본 |
| `근태기록 26년 8월/` (`.xls` 5개) | 붙여넣을 원본 |
| `강사용_정답표_2026-08.txt` | 대조용 실제 숫자 — **수강생에게 주지 마세요** |

초기화한 내용은 셋입니다.

1. **근태원본** — 8월 블록(2~156행)을 지우고 6·7월을 **2~299행으로 당겨** 붙였습니다.
   그래야 `Ctrl`+`↓` 로 찾은 **다음 빈 행이 300행**이 되어,
   문서에 적힌 "맨 아래 빈 행에 붙여넣기"와 화면이 일치합니다.
2. **월별입력** — 8월 줄의 `D`(근무일수)·`E`(주차일수)만 비웠습니다.
   비고·유예시간 같은 **standing 설정은 그대로 두었습니다.**
   사용안내에 적힌 매월 3단계와 정확히 같은 것만 비운 것입니다.
3. **월설정 B1** — `2026-08` → `2026-07`.
   붙여넣기 전에는 7월 명세서가 보이고, 3단계를 마치면 8월로 바뀝니다.

값만 옮기고 수식은 건드리지 않았습니다(`P~X` 열은 4001행까지 이미 깔려 있음).
`wb.calculation.fullCalcOnLoad = True` 를 세워 열 때 다시 계산하게 했습니다.

### 검증한 것

예제 파일 사본에 **3단계를 실제로 실행하고 Excel(COM)으로 재계산**해서
원본의 8월 계산값과 대조했습니다.

- 6명 × 25개 항목 = **150개 대조, 불일치 0개**
- 차인지급액 6명 전부 원본과 일치
- 명세서 제목이 `급 여 명 세 서 (2026년 8월)` 로 전환됨
- openpyxl 왕복 손실 점검 — 시트 이름·정의된 이름·조건부 서식·
  데이터 유효성·주석 9개·인쇄 영역 **모두 보존**

### 개인정보 — 반드시 지킬 것

급여대장에는 **주민등록번호와 개인별 급여액**이 들어 있습니다.
GitHub 저장소(`inchoriya1/sumgo6615`)가 **공개**이므로:

- `.gitignore` 에 **`엑셀자료/`** 를 넣었습니다. 절대 풀지 마세요.
- 강의안 12장에도 이 파일을 다루는 **보안 주의**를 한 상자 넣었습니다.

**이 강의안은 처음에 실명을 가려서(`김○○`) 만들었는데,
사용자가 "실명 넣어서 더 구체적으로, 배포도 해야 한다"고 해서 실명으로 바꿨습니다.**
그리고 공개 저장소에 실명을 평문으로 둘 수 없으니
**암호를 걸어 내보내는 방식**으로 갔습니다. 사용자가 고른 암호는 **`6615`** 입니다.

| 파일 | 무엇 | 커밋되나 |
|---|---|---|
| `excel-4.src.html` `excel-5.src.html` | **원본.** 실명이 든 강의안 | ✗ (`*.src.html` 무시) |
| `excel-4.html` `excel-5.html` | 원본을 잠근 것. 자물쇠 화면 + 암호문만 | ✓ |
| `tools/build-locked-page.py` | 원본 → 잠근 파일 | ✓ |

**고칠 때는 반드시 `.src.html` 을 고치고 다시 만드세요.**
`excel-4.html` 을 직접 고치면 다음 빌드에 통째로 날아갑니다.

```
python tools/build-locked-page.py excel-4.src.html excel-4.html 6615
python tools/build-locked-page.py excel-5.src.html excel-5.html 6615
```

- 자바스크립트로 **숨기기만 하는 방식은 쓰지 않았습니다.** 소스 보기 한 번이면
  다 읽혀서 아무 소용이 없고, 오히려 안전하다고 착각하게 만듭니다.
  본문을 **실제로 암호화**합니다(PBKDF2-SHA256 31만 번 + AES-256-GCM).
- 빌드 도구가 결과 파일에 실명이 남았는지 **스스로 검사하고, 남아 있으면 멈춥니다.**
- **네 자리 암호는 약합니다.** 검색엔진·크롤러·지나가는 사람을 막는 수준이고,
  작정하고 덤비는 사람은 뚫습니다. 사용자에게 이 점을 알렸습니다.
- **개인별 급여 금액은 잠긴 쪽에도 넣지 않았습니다.** 대조용 숫자는
  예제 폴더의 `강사용_정답표_2026-08.txt` 에만 있습니다.
  근태 시간(136H 등)은 검산에 필요해서 강의안에 넣었습니다.
- `excel.html`·`index.html` 카드에는 **회사 이름을 쓰지 않습니다.**
  그 두 쪽은 암호 없이 공개됩니다.
- `file://` 로는 안 열립니다(브라우저가 Web Crypto 를 막음).
  내 컴퓨터에서 볼 때는 `.src.html` 을 여세요.

### 사이트에 붙인 자리

- `excel.html` — **"세 부" 절과 별도로** `실무 편` 절을 하나 두고 카드를 붙였습니다.
  세 부 구성 이야기를 흐리지 않기 위해서입니다.
  머리말 `구성`도 `세 부 + 실무 편` 으로 고쳤습니다.
- `index.html` — 엑셀 카드 배지를 `3부 + 실무 편` 으로.

## 새로 만든 것 — 엑셀 실무 2일차 (`excel-5.html`)

1일차가 **매달 똑같이 하는 일**이었다면, 2일차는 **가끔 생기는 일**입니다.
사용자 요청은 "2일차 강의안도 만들어줘" 한 줄이었고,
1일차 마지막에 예고해 둔 내용(입·퇴사, 중도 입퇴사 달의 셈)을 축으로 잡았습니다.

### 장 구성 (14장, 120분)

`00 준비` → `01 오늘 배울 것` → **`02 입사` · `03 일할 계산` · `04 퇴사`** →
`05 임금 인상` → `06 더 주는 것과 빼는 것` → `07 4대보험` → `08 소득세` →
`09 지난달 고치기(소급 정산)` → `10 한 달을 닫는 순서` →
`11 연말과 파일 한도` → `12 문제 해결` → `13 확인 목록`.

**축이 되는 문장 하나** — *직원설정은 계속 그럴 것, 월별입력은 이번 달만.*
`직원설정` 에는 연월이 없어서 한 칸을 고치면 **스물네 달이 다 바뀝니다.**
이 이야기가 01·04·05·08·12장에 반복해서 나옵니다. 고칠 때 이 축을 흐리지 마세요.

### 파일을 실제로 돌려서 확인한 것

강의안에 적은 숫자와 주장은 **전부 Excel(COM)로 실행해 확인했습니다.**

- **일할 계산** — `기본근로시간 = 209 × 재직일수 ÷ 그 달 일수`.
  파일 안 실제 사례(월별입력 45행, 6/8 입사)가 `209×23/30 = 160.2333` 로 맞습니다.
- **신규 입사 실습**(가상 인물, 9/14 입사, 시급 13,000) — 넣고 돌린 결과
  지급액계 **1,929,630** / 차인지급액 **1,892,930**. 강의안의 숫자가 이것입니다.
- **퇴사 함정** — `직원설정!F` 퇴사일자는 **어느 수식에도 쓰이지 않습니다.**
  퇴사일을 적어도 급여가 **1원도 안 줄어드는 것**을 실제로 확인했습니다.
  멈추려면 **근무일수를 비워야** 합니다. 04장 전체가 이 이야기입니다.

### 실습 자료 — `엑셀자료/강의2일차_예제/`

| 파일 | 무엇 |
|---|---|
| `…_급여대장_연간관리_예제_2일차.xlsx` | **1일차를 마친 상태** |
| `강사용_정답표_2일차.txt` | 입력값·기대값·시연 순서 — **수강생에게 주지 마세요** |

2일차 예제는 **1일차 예제에 세 단계를 실제로 실행해서** 만들었습니다
(`scratchpad/build2.py`). 8월 차인지급액이 원본과 여섯 명 모두 일치합니다.

### 개인정보 처리 — 1일차와 같습니다

- 원본 `excel-5.src.html` 은 `.gitignore`(`*.src.html`), 공개되는 것은 잠근 `excel-5.html`.
- **실습 금액은 가상 인물(홍길동)의 것**이라 강의안에 넣었습니다.
  실재하는 분의 급여 금액은 여전히 넣지 않았습니다.
- 근태 시간·부서·급여방식·보험 적용 여부는 검산에 필요해서 넣었습니다.

### 강의 중 발견해서 강의안에 넣은 '확인할 것' 셋

이 회사가 실제로 확인해야 할 일들입니다. 13장 마지막 상자에 적어 두었습니다.

1. **사용안내 시트의 확인 필요 사항 네 줄** — 09장 소급 정산으로 처리할 것들.
2. **간이세액표가 오래된 것으로 보입니다.** 표 제목 칸에 몇 해 전 연도가 적혀 있습니다.
   맞다면 매달 소득세가 조금씩 어긋나고 연말정산에서 몰아서 정산됩니다.
   **단정하지 않고 "확인해 보세요"로 적었습니다.**
3. **`근태기록 26년 6월` 폴더에 6/22 퇴사자 파일이 있는데 급여대장에는 그분이 없습니다.**
   다른 데서 정산했을 수도, 빠졌을 수도 있습니다.

## 간이세액표 — 지금 것은 2021년판입니다

**확인한 사실입니다.** `간이세액표1` 시트 A1 제목 칸 끝에 `2021` 이 붙어 있고,
값도 2021년판입니다(월급여 3,000천원·가족 1명 → 74,350원).

국세청은 그 뒤로 **2023년 개정**, **2026년 개정**을 냈고,
**2026년 개정판은 2026년 3월 지급분부터** 적용됩니다.
이 파일이 만들고 있는 **6·7·8·9월 급여가 전부 그 대상**이므로,
**지금 떼고 있는 소득세는 실제와 다릅니다.**
(매달 떼는 것은 미리 내는 돈이라 연말정산에서 결국 맞춰지지만,
그때 몰아서 더 내거나 돌려받게 됩니다.)

**표 데이터는 제가 구하지 못했습니다.** 국세청 다운로드 링크는
세션 없이 직접 부르면 400으로 막힙니다. 세액표는 지어낼 수 없는 자료라
**받아오면 한 번에 갈아 끼우는 도구만 만들어 두었습니다.**

### `tools/swap-tax-table.py`

사용법과 검사 항목은 `README.md` 를 보세요. 여기서는 **왜 도구가 필요한지**만 —
소득세 수식에 `$A$3:$N$591` 로 **행 번호가 박혀** 있고 그 수식이
`기본정보` 시트에 **192곳** 있습니다. 줄 수가 달라지면 192곳을 함께 고쳐야 합니다.

**시험해 본 것**

- **왕복** — 지금 표를 뽑아 다시 넣었더니 589줄 × 13칸 **차이 0개**.
  엑셀로 열어 재계산했을 때 8월 소득세·지방소득세·차인지급액이
  여섯 명 모두 원본과 일치.
- **줄 수 변경** — 399줄짜리를 넣었더니 192곳이 전부 `$N$401` 로 바뀜.
- **망가진 표 3종** — 구간 순서 뒤바뀜 / 사이 빈틈 / 가족수 열 뒤집힘.
  셋 다 잡아내고 **만들지 않고 멈춥니다.**
- **마지막 줄** — 원래 표의 끝은 `10000 이상 10000 미만` 입니다.
  처음에 이 줄을 걸러 버려 588줄이 나왔고(고액자 세액이 달라짐),
  판정을 `<` 에서 `<=` 로 고쳐 잡았습니다. 그 형태가 아니면 경고합니다.

### 곁들여 찾은 것 — 1천만원 초과 구간

표가 1천만원에서 끝나서, 그보다 많이 받는 사람은 `VLOOKUP` 이
**마지막 줄 값에서 멈춥니다.** 국세청은 그 구간을 별도 산식으로 정하는데
이 파일에는 없습니다. **지금 해당하는 분이 한 명 있습니다.**
표를 갈아 끼워도 이건 안 고쳐집니다 — **수식 자체를 바꿔야** 합니다.

## 세액표 상한 초과 — 산식 대신 '경고'로 고쳤습니다

사용자 요청은 "1천만원 초과 구간 수식도 고쳐줘" 였습니다.
**산식은 넣지 않았습니다.** 국세청·법제처 원문 모두 접근이 막혀
(다운로드 400, 법령 페이지는 JS 게이트) 공식 산식을 확인하지 못했고,
**세금 산식은 기억으로 지어 넣을 수 있는 것이 아닙니다.**

### 먼저 확인한 사실 — 지금 틀린 사람은 없습니다

대표 월급여액이 **정확히 10,000,000원**입니다. 표의 마지막 줄이
`10000 이상 10000 미만` 이라 **제대로 맞는 자리**입니다.
24개월 × 8명 중 상한을 **넘는 줄은 0개**, **딱 걸린 줄이 3개**(6·7·8월).

그러니 앞선 세션에서 강의안에 쓴 *"지금 해당하는 분이 한 분 있습니다"* 는
부정확했습니다. **고쳤습니다.** 문제는 *지금*이 아니라
**상여가 한 번만 붙어도 넘어간다**는 것입니다.

### `tools/fix-over-cap.py` 가 하는 일

사용법은 `README.md` 를 보세요. 요지는 **금액을 바꾸지 않고,
모르고 지나가지 못하게** 만드는 것입니다.

1. `월별입력` 에 **`소득세직접입력`(X열)** 을 만듭니다 → 적으면 표 대신 그 값.
2. `기본정보` **`성명확인`(BD열)** 에 `★소득세 확인필요` 경고.
   **1일차 06장 검산에서 이미 보는 자리**라 새 절차를 안 만들어도 됩니다.

상한은 `세액표상한` = `MAX(간이세액표1!$A$3:$A$100000)` 이름으로 두었습니다.
**표를 갈아 끼우면 상한도 따라 바뀝니다.**

### 엑셀로 확인한 것 (4가지)

1. **손 안 댔을 때** — 8월 소득세·공제합계·차인지급액이 여섯 명 모두 원본과 일치(불일치 0).
2. **상여 300만원 → 월급여 1,300만원** — 확인 열에 경고가 뜸.
   소득세는 여전히 1,507,400(표 마지막 값)이지만 **이제 보입니다.**
3. **소득세직접입력에 2,400,000 입력** — 그 값이 쓰이고,
   지방소득세가 240,000(10%)으로 따라오고, 공제합계 3,168,750 ·
   차인지급액 9,831,250 로 맞고, **경고가 사라지고**, 명세서에도 반영.
4. **상여를 도로 빼면** 원래 값으로 정확히 복귀.

2일차 예제에도 적용해서, **홍길동 실습 숫자가 그대로**인 것을 확인했습니다
(월급여액 1,929,630 / 차인지급액 1,892,930).

### 1일차 예제 — 적용 완료, 그리고 조회월을 되돌렸습니다

처음에는 사용자가 엑셀로 열어 두고 있어(`~$` 잠금 파일) 건드리지 않았고,
닫은 뒤 적용했습니다. **두 예제 모두 보완이 들어가 있습니다.**

적용하며 한 가지를 함께 되돌렸습니다 — 파일을 열어 보니
**조회월이 `2026-08` 로 바뀌어** 있었습니다(실습은 안 한 상태:
근태원본 299행 그대로, 8월 근무일수 비어 있음).
강의안 05장이 *"B1 칸에 `2026-07` 이라고 적혀 있습니다"* 로 시작하므로
**`2026-07` 로 되돌렸습니다.**

배포 상태를 다시 확인한 결과 — 조회월 2026-07 · 근태원본 299행 ·
7월 명세서 · 8월 슬롯 전부 0 · `소득세직접입력` 칸 있음 · 경고 없음.
그 상태에서 **3단계를 실행하니 정답표 78개 항목이 전부 일치**했습니다.

### 실제 파일에 적용한 보완본

`엑셀자료/…_급여대장_연간관리_상한초과보완.xlsx` 로 만들어 두었습니다.
**원본은 그대로입니다.** 사용자가 확인하고 갈아탈지 정할 일입니다.

## 시트를 '골라서 쓰는' 방식으로 바꿨습니다 (18장 → 14장)

사용자 요청 두 가지 — **근태원본에 사람을 골라서 넣게**,
**근퇴-xxx 6장을 한 장으로**.

### ① 근퇴계 통합 — 깔끔했습니다

6장이 **딱 세 칸만** 달랐습니다(A1 제목 · N1 슬롯 · G4 급여방식).
'이 름' 칸(C3)을 **고르는 칸**으로 바꾸니 나머지 수식이 이미 `$C$3` 을
쓰고 있어서 **손댈 것이 없었습니다.**

**6명 × 327칸 = 1,962칸을 원본 6장과 대조해 차이 0개.**
다른 시트가 근퇴-xxx 를 참조하는 곳도 없었습니다(사용안내 본문 한 줄뿐, 그것도 고침).

### ② 근태넣기 — 요청을 그대로는 못 했습니다

> **이 시트는 뒤에 `근태점검` 이 됐습니다** — 아래 «시트 정리» 를 보세요.
> 여기부터 이어지는 이야기는 **그때의 기록**이라 그대로 둡니다.

**근태원본 자체를 사람별로 바꾸지는 않았습니다.** 이유가 중요합니다 —
근태원본은 **모든 사람·모든 달을 쌓아 두는 창고**입니다.
사람을 고를 때마다 내용이 바뀌게 하려면 수식으로 끌어와야 하는데,
그 순간 **저장이 아니라 '보기'가 되어 지난달 기록이 사라집니다.**
매크로 없는 `.xlsx` 라 단추로 옮겨 담을 수도 없습니다.

그래서 **창고는 그대로 두고 창구를 따로** 만들었습니다.
연월·사람을 고르면 붙여넣을 자리를 알려 주고, 넣은 뒤 검사하고,
아래 **연월 × 사람 격자**로 빠진 것을 보여 줍니다.

**판정이 헛경고를 내지 않게 두 번 다듬었습니다** —
처음 판은 중도 입사자(6/8 입사, 23줄)와 대표(근태 없음)에게
"줄 수가 안 맞습니다"를 띄웠습니다. 지금은 급여방식이 고정월급이면
채근하지 않고, 적을 때는 "중간에 들어오거나 나갔으면 정상입니다"라고 말합니다.

### 적용 상태

| 파일 | 상한 보완 | 시트 정리 |
|---|---|---|
| `…_상한초과보완.xlsx` (실제 파일 사본) | ✓ | ✓ |
| 1일차 예제 | ✓ | ✓ |
| 2일차 예제 | ✓ | ✓ |

**셋 다 끝났습니다.** 1일차 예제는 사용자가 엑셀을 닫은 뒤 적용했습니다.

적용 뒤 **1일차 강의를 처음부터 끝까지 돌려서 확인**했습니다 —
근태넣기가 알려 주는 칸(A300 → A331 → A362 → A393 → A424)에
사람마다 붙여넣으니 다섯 번 모두 판정이 **맞습니다**로 뜨고,
근무일수·조회월까지 마친 뒤 **정답표 78개 항목이 전부 일치**했습니다.
근퇴계도 사람을 바꿔 가며 제대로 나옵니다.

### 강의안도 함께 고쳤습니다

- 1일차 — 시트 수(열여덟→열네), 02장 시트 표에 `근태넣기` 추가,
  **03장 붙여넣기 절차를 근태넣기 기준으로 다시 씀**(Ctrl+↓ 방법도 남겨 둠 —
  다른 회사 파일에는 이 시트가 없으므로), 05장 표의 `근퇴-이름`→`근퇴계`,
  **06장 검산이 네 곳 → 다섯 곳**(근태넣기 표 훑기가 맨 앞), 확인 목록 3줄 추가.
- 2일차 — 01장 지도에 근태넣기·근퇴계 한 줄, 10장 '한 달을 닫는 순서'에 반영.
- `excel.html` 카드의 '시트가 열여덟 장' 도 고침.

## 근태원본 개편 — 출퇴근 두 열만 넣으면 됩니다

사용자 요청: "출근시간과 퇴근시간만 복사해서 붙여넣기 하면 되게, 사용하기 쉽게".

### 먼저 규칙을 찾아냈습니다

출퇴근만으로 기본·연장을 내려면 **계산 규칙**을 알아야 하는데 문서가 없었습니다.
그래서 **근태기록 .xls 16개 475줄을 다 읽어 규칙을 역산하고 대조**했습니다.

| | 기본 | 연장 |
|---|---|---|
| 평일 | 8시간 | `(퇴근 − 17:30)` 30분 단위 내림 |
| 휴일 | 0 | `(퇴근 − 08:30)` 30분 단위 내림 |

**472/475 일치.** 안 맞는 3줄은 전부 **사람이 판단해 손으로 깎은 지각**이었습니다
(10:59 출근 → 3H, 11:11 → 3.5H, 13:51 → 5H — 자동 계산이 아닙니다).
그래서 **O열 `지각차감(H)`** 를 만들고, 개편할 때 기존 3줄을 그리로 옮겨 담았습니다.

**근무일명칭**도 역산했습니다 — 월~금은 `평일`, 토·일은 `휴일`,
공휴일이면 그 이름(현충일·광복절·제헌절·지방선거일·대체공휴일).
그래서 **공휴일 표**가 필요해졌고 `월설정` G열에 만들었습니다.

### 구조

연월 × 사람 × 31일 자리를 **미리 깔았습니다**(8 × 24 × 31 = 5,952줄).
블록 시작 = `2 + ((월인덱스−1)×8 + (사람인덱스−1)) × 31`.
자리가 고정이라 **두 번 넣어도 덮어써질 뿐** 두 배가 되지 않습니다.

### 겪은 문제 셋 (다시 만나면 바로 알아보시라고)

1. **공휴일이 안 잡혔습니다** — 표에 날짜를 **글자**로 넣어서 `MATCH` 가 실패했고,
   6/6 현충일이 '휴일 + 토요일' → **토요**로 잡혔습니다(정답은 특근).
   140개 불일치의 원인. **진짜 날짜로** 넣어 해결.
2. **지각차감이 안 옮겨졌습니다** — 기본 시간이 `datetime.timedelta` 인데
   `.hour` 로 읽으려다 실패. 타입 네 가지(timedelta·time·숫자·글자)를 다 받게 고침.
3. **"재계산 6.5초"는 헛것이었습니다** — 제 검증 코드가 COM으로 5천 칸을
   하나씩 읽던 시간이었습니다. **실제 계산은 0.17초**입니다.
   붙여넣고 다시 계산 0.07초. 속도 문제는 없습니다.

### 확인한 것

- **4,032개 대조, 불일치 0개** (3개월 × 6명, 급여 22항목 + 근태집계 6항목)
- **실제 흐름 재현** — 블록을 비우고 `.xls` 출퇴근 두 열만 붙여넣으니
  근태집계가 원래 값으로 정확히 복귀
- **두 예제 모두** 강의안 숫자 그대로 (1일차 3단계 후 정답표 일치, 2일차 홍길동 1,892,930)

### 강의안

1일차 03장을 **거의 다시 썼습니다** — 자리 찾기가 '자리 확인'으로 바뀌고,
'열네 개 열 복사'가 '출근·퇴근 두 열 복사'로 바뀌었습니다.
`왜 출근·퇴근만 넣어도 되나`, `지각을 깎을 때만 한 칸 더`,
`공휴일 표를 꼭 채워 두세요` 상자를 새로 넣었습니다.
12장 문제 해결에서 **'두 번 붙여넣기'가 빠지고**(구조상 안 생김)
'하루씩 밀림'과 '공휴일 누락'이 들어갔습니다.

## 한 사람만 골라서 넣기 — 자동 필터

사용자 요청: "월과 사람을 선택하면 거기에 출근/퇴근 입력",
"한 시트에 여러 명이 한꺼번에 나오면 선택해서 한 명만".

앞 단계에서 자리를 미리 깔아 5,952줄이 되면서 **여덟 사람 스물네 달이
한꺼번에 보이는** 문제가 생겼습니다. 그걸 푼 것입니다.

### 왜 드롭다운 한 칸이 아니라 필터인가

**칸을 골라 화면이 바뀌게 하려면 매크로가 필요합니다.**
`.xlsm` 으로 바꾸면 보안 경고가 뜨고 메일·공유에서 차단되기도 해서,
매크로 없는 `.xlsx` 를 유지하고 **엑셀이 원래 가진 자동 필터**를 썼습니다.

슬라이서(클릭 한 번에 걸리는 것)도 검토했지만 openpyxl 이
`slicerCache` 파트를 못 써서 포기했습니다.

### 한 일

- `근태원본` 에 자동 필터(`A1:X5953`), **A열 이름 + Q열 연월**로 걸러 둡니다.
- 저장할 때 **행 `hidden` 상태까지 함께** 써서, 열자마자 31줄만 보입니다.
- 처음 걸어 두는 사람은 **고정월급이 아닌 첫 사람**입니다.
  (처음엔 대표가 걸려 빈 화면만 나왔습니다 — 고쳤습니다.)
- `근태넣기` 를 **현황판**으로 바꿨습니다. 고르는 칸을 없애서
  '고르는 곳이 두 군데'인 혼란을 없앴습니다.

> **뒤에 `근태점검` 으로 이름을 바꾸고, 필터는 `A1:Y5953` 으로 늘었습니다**
> (Y열 `조회월` 추가) — 아래 «시트 정리».

### 확인한 것

- 열자마자 **31줄만** 보임 (고정월급이 아닌 첫 사람 · 2026-08)
- 그 자리를 비우고 `.xls` 출퇴근을 넣으니 근태집계가 원래대로
- **이름 필터만** 바꾸면 다음 사람 31줄, 연월을 2026-09 로 바꾸면 **30줄**
- 1일차 예제를 배포 상태에서 **필터로 사람만 바꿔 가며** 다섯 명 넣고
  근무일수·조회월까지 마치니 **정답표 불일치 0개**
- 붙여넣고 다시 계산 0.07초

### 또 되돌린 것

1일차 예제의 조회월이 **또 `2026-08` 로 바뀌어** 있어서 `2026-07` 로 되돌렸습니다.
(사용자가 파일을 열어 눌러 보시는 듯합니다. 강의안 05장이 2026-07 에서 시작합니다.)
**배포 전에 이 값을 꼭 확인하세요.**

## 2026년 공휴일 표를 채웠습니다

`월설정` G열에 **스물한 개**. 근무일명칭이 이 표로 정해지고 그것이
수당 종류(연장 / 토요 / 특근)를 가르므로, **빠지면 조용히 틀립니다.**

### 날짜를 어떻게 정했나

- 날짜 고정인 것은 그대로. **음력인 것**(설날 2/17 · 부처님오신날 5/24 ·
  추석 9/25)은 2026년 양력 날짜를 확인해 넣었습니다.
- **대체공휴일은 2026년에 네 번** — 3/2 · 5/25 · 8/17 · 10/5.
  (삼일절·부처님오신날이 일요일, 광복절·개천절이 토요일과 겹침.)
- 요일을 전부 계산해서 연휴 구성과 맞는지 교차 확인했습니다.
- **결정적 확인** — `8/17 대체공휴일`이 이 회사 근태기록에 **실제로 찍혀
  있습니다.** 제가 세운 규칙이 맞다는 것이 회사 자료로 증명된 셈입니다.

### 제헌절 — 2026년부터 다시 공휴일입니다

처음에 **"나라의 공휴일이 아닌데 이 회사만 쉰다"고 잘못 적었습니다.**
사용자가 바로잡아 주었습니다 — **제헌절은 2026년부터 다시 법정공휴일**입니다.
2026년 2월 국무회의에서 개정안이 의결되어 18년 만에 부활했고,
이로써 5대 국경일이 모두 공휴일이 되었습니다.

그러니 **회사에 "쉬느냐"고 물을 일이 아니라 해마다 자동으로 들어갑니다.**
법정공휴일이므로 **대체공휴일 규칙도 적용됩니다**(토·일과 겹치면 다음 평일).
2026년 7월 17일은 금요일이라 대체가 없지만, **2027년부터는 요일을 확인**하세요.

### 확인한 것

- 표 21개, 근태원본에서 **11개 날짜를 찍어 근무일명칭을 대조 — 전부 일치**
  (신정·설날·대체공휴일·어린이날·현충일·광복절·추석·성탄절 …)
- 대조군(1/2, 9/28)은 **평일**로 나옴
- **8월 급여는 그대로** (정답표 불일치 0개) — 6~8월에 새로 끼는 공휴일이
  없기 때문입니다. 도구가 이걸 먼저 검사하고 알려 줍니다.

## 근태원본 서식·단위, 그리고 크게 잡은 버그 하나

사용자 지적: "근무일자가 46235 로 나온다", "기본·연장이 소수로 나온다".

### 고친 것

| | 전 | 후 |
|---|---|---|
| 근무일자 | `46235` | `2026-08-08 (토)` |
| 출근·퇴근 | `0.325` | `07:48` |
| 기본·연장 | `0.3333` | `8` · `3` |

기본·연장은 **값 자체를 시간으로** 바꿨습니다(`8/24` → `8`).
이 두 열을 쓰는 곳이 같은 시트의 `S`·`T` 뿐이어서 거기서 `×24` 만 뺐습니다.
**밖으로 나가는 값은 안 바뀝니다** — 8월 정답표 불일치 0개로 확인.

두 번 헛디뎠습니다 —
문자열 끝을 잘라 붙이려다 `/24)` 를 못 잡아 **K·L 은 그대로인데 S·T 만
바뀌는** 상태를 만들었습니다(그 사이 값이 틀렸습니다). 수식을 통째로 다시
쓰는 방식으로 바꿔 해결. 그리고 `0.##` 서식은 8 을 **`8.`** 로 보여 줘서
`General` 로 바꿨습니다.

### ★ 곁들여 잡은 진짜 버그 — 2027년 5월부터가 죽어 있었습니다

`rebuild-attendance.py` 로 자리를 5,952줄로 늘리면서
**`P~W` 파생 열(날짜·연월·요일·구분·월키·일키)을 안 늘렸습니다.**
원본이 `4001` 행까지만 가지고 있었기 때문입니다.

**4002행부터 = 2027년 5월부터 월키가 없어 근태집계가 0, 급여가 통째로 0**
이 되는 상태였습니다. **오류는 안 뜹니다.**
서식 확인하러 열 수를 훑다가 `8000칸` 이라는 이상한 숫자가 나와서 잡았습니다
(11,904 여야 하는데 8,000 = 4,000줄 × 2).

`fix-formats.py` 가 빈 칸 **11,712칸**을 채웠습니다. 확인 결과
4002행 `2027-05-02`, 5000행 `2027-09-08` 로 정상.

**교훈 — 행을 늘리는 도구는 모든 파생 열이 같은 행까지 있는지 반드시 확인할 것.**

### 또 되돌린 것 (세 번째)

1일차 예제의 조회월이 이번엔 **`2026-06`** 으로 바뀌어 있었습니다.
`2026-07` 로 되돌렸습니다. 강의안 05장이 7월에서 시작합니다.
**사용자가 예제를 열어 보실 때는 저장하지 말고 닫는 편이 좋습니다.**

## 근무일수 자동 채우기 — 그리고 퇴사 함정이 사라졌습니다

사용자 질문: "근태기록을 넣으면 월별입력도 자동으로 채워지게 할 수 있나?"
→ **부분적으로 가능**하다고 답하고, 사용자가 '자동 채우기 + 입·퇴사일로 켜고 끄기'를 골랐습니다.

### 자료로 먼저 따진 것

- **근태에서는 안 나옵니다.** 8월 근무일수 26 vs 실제 출근 17.
  급여 지급 기준일이지 출근일이 아닙니다.
- **달력에서는 나옵니다.** 209H·일수는 `그 달 날수 − 일요일 수`.
  13건 중 10건 정확 일치, 안 맞는 3건은 전부 비고에 이유가 적혀 있었습니다.
- **일급제는 달력과 무관**한 합의값(21+5)이라 직원설정에 칸을 만들었습니다.

### 켜는 신호는 근태입니다

`근태집계` 에 `근태들어옴`(출퇴근이 찍힌 날 수) 열을 만들어,
**그 달 근태가 있어야 근무일수가 들어가게** 했습니다.
그래야 **미래 달이 미리 계산되지 않습니다.**

**고정월급(대표)은 자기 근태가 없습니다.** 그래서
**그 달에 누구든 근태가 들어왔는지**(`근태집계` M열)로 켭니다.
회사가 그 달에 돌아갔으면 대표도 급여를 받으니까요.

세 판을 거쳤습니다 —
① `OR(고정월급, 근태>0)`: 재직이면 무조건이라 **미래 달까지 다 찍힘**
② `근태>0` 만: 대표는 영영 0이라 **한 칸을 손으로 적어야 함**
③ `OR(자기 근태>0, AND(고정월급, 그 달 회사 근태>0))`: **손으로 넣는 값 0개**

### ★ 퇴사 함정이 사라졌습니다 — 2일차 04장을 다시 썼습니다

**입사일·퇴사일이 이제 실제로 계산을 켜고 끕니다.**
전에는 퇴사일이 표시용이라 "적어도 안 멈춘다"가 04장의 핵심 교훈이었는데,
이제 **"적으면 그다음 달부터 멈춘다"** 가 되었습니다. 장을 통째로 다시 썼습니다.

대신 새 함정이 하나 생겼습니다 — **퇴사한 마지막 달은 자동값이 한 달 치(26)**
로 들어옵니다. 실제 일한 날수로 고쳐 적어야 합니다. 그 경고를 넣었습니다.

### 강의안 변화

- **1일차** — `② 근무일수를 넣기` → `② 근무일수 확인하기`.
  **3단계가 2단계**가 됐습니다 — 근태 붙여넣기 → 조회월 바꾸기.
  손으로 넣는 값이 하나도 없습니다.
- **2일차 04장** — 퇴사 함정 → 퇴사 처리 절차로 전면 개편
- **2일차 05장** — "같은 짜임이 여덟 개" → **열 개**(근무일수·주차일수 추가)

### 확인한 것

- 6·7·8월 불변(정답표 0), 9월은 근태 전 0 → 넣으면 26/21+5
- 퇴사일 넣으면 그 달은 나오고 다음 달부터 0, 과거는 불변
- **1일차 강의를 근태만 넣고 조회월만 바꿔 돌려 정답표 불일치 0개**
  (손으로 넣은 값 0개)
- 대표는 9월 근태 넣기 전 0 → 다섯 명 근태를 넣으니 26으로 함께 켜짐
- 10월·2027-12 등 근태 없는 달은 여전히 0
- **2일차 홍길동 실습도 그대로** — 근무일수 15 / 차인지급액 1,892,930

## 시트 정리 — 근태넣기가 근태점검이 됐습니다

사용자의 물음 둘에서 시작했습니다.
**"월설정에서 달을 바꿔도 근태원본 근무일자가 안 바뀌는데?"** 와
**"근태넣기 시트가 꼭 필요해?"**

### 첫 물음 — 안 바뀌는 것이 맞습니다

`근태원본` 은 조회월 한 달짜리가 아니라 **24개월 × 8명 × 31일 = 5,952줄**이
깔린 영구 장부입니다. 각 줄의 날짜는 자기 블록의 달(`월설정!$B$4`~`$B$27`)에
고정으로 물려 있습니다.

**바꾸면 안 됩니다.** 출근·퇴근은 수식이 아니라 직접 친 값이라,
날짜가 조회월을 따라 움직이면 **8월에 넣은 출퇴근이 9월로 바꾸는 순간
9월 근태가 됩니다.** 지난 달 기록이 통째로 망가집니다.

대신 **찾아가기 쉽게** 만들었습니다 — `근태원본` **Y열 `조회월`**.
`월설정` B1 의 달에만 `◀` 가 켜져, **필터 하나로 그 달 248줄**이 나옵니다.
전에는 연월·이름 두 번 걸어야 했습니다.

### 둘째 물음 — 계산에는 안 쓰입니다

수식 전체를 훑어 확인했습니다. **`근태넣기` 를 참조하는 시트는 하나도 없습니다**
(`사용안내` 에 이름만 언급). 지워도 급여는 한 원도 안 바뀝니다.

다만 격자는 값어치가 있어 **남겼습니다** — 5,952줄에 필터로 밀어 넣다 보면
"8월 그 사람 넣었던가"를 확인할 방법이 사실상 그것뿐입니다.
**이름이 넣는 곳처럼 보이는 것이 문제**라고 보고 `근태점검` 으로 바꿨고,
공휴일 개수 줄은 `월설정` G열을 직접 보면 되는 내용이라 뺐습니다.

### 한 일 (`tools/tidy-sheets.py`)

- `사용안내` — *"맨 아래 빈 행에 붙여넣기 · 계속 쌓아 두면"* 이라는 **옛 방식**이
  그대로 적혀 있었습니다. 시트는 이미 고정판으로 바뀌었는데 안내만 뒤처져 있었고,
  **사용자가 헷갈린 원인이 이것으로 보입니다.**
- `근태넣기` → `근태점검`, 격자만 남김
- `근태원본` Y열 `조회월` 5,952줄

### ★ 곁들여 잡은 것 — 조건부서식이 4001행에서 끊겨 있었습니다

블록 줄무늬와 경계선이 **2027년 초부터 사라지는** 상태였습니다.
4000행짜리이던 시절의 자취로, `fix-formats.py` 가 파생 열은 5953행까지
늘렸지만 **조건부서식 범위는 같이 안 늘린** 것입니다.
`A2:X4001` → `A2:Y5953`.

곁들여, `사용안내` 시트 목록의 `근태원본` 과 `근태넣기` 가 **한 칸에 두 줄**로
들어가 있었는데 줄바꿈 설정이 없어 **둘째 줄이 화면에 안 보였습니다.**

### 강의안 변화

- 1일차(`excel-4`) — 02장 시트 표, 06장 검산 1·2번, 마무리 확인 목록에서
  이름 교체. **03장에 「더 빠른 길 — Y열 조회월」 안내 상자 신설**,
  확인 목록에 Y열 항목 한 줄 추가.
- **06장 검산은 다섯 곳 그대로 뒀습니다.** 2번(공휴일)을 지우면 네 곳이 되는데,
  빠지면 수당 종류가 조용히 틀어지는 항목이라 **`월설정` G열을 보도록 돌렸습니다.**
- 2일차(`excel-5`) — 01장 시트 지도 한 줄.

### 확인한 것

- 백업과 한 칸씩 대조 — **근태원본 입력칸 775개(예제 525개) 전·후 동일, 다른 칸 0개**
- **나머지 12개 시트 전부 값이 달라진 칸 0개**
- `◀` 켜질 행 — 조회월 `2026-08` → 1738~1985행 **248줄**(8명 × 31일),
  예제(`2026-07`) → 1490~1737행 248줄
- 두 파일 다 `fullCalcOnLoad` 켜져 있어 엑셀에서 열면 자동 재계산됩니다
- 강의 페이지를 다시 빌드하고 **실제로 암호(`6615`)를 넣어 복호화된 본문**에서
  `근태넣기` 0회 · `근태점검` 3회(excel-5는 1회) 확인

### 못 한 것

- **LibreOffice가 이 컴퓨터에 없어 실제 계산값까지는 못 돌려 봤습니다.**
  수식 구조와 참조 대상만 확인했습니다.
- 강의 페이지 스크린샷 — 페이지가 33,000px로 길어 브라우저 패널이
  깊은 위치에서 빈 화면만 잡았습니다. 복호화된 DOM 텍스트로 대신 확인했습니다.

## 요일 색 · 소수로 보이던 시각 · 근퇴계 가로 인쇄

사용자의 요청 셋입니다.
**"토요일 파랑, 일요일·공휴일 빨강"**,
**"24로 나눠서 소수점 나오는 것 찾아서 시간으로"**,
**"근퇴계 가로로, 한 페이지에"**.

### ★ 소수점 — `/24` 수식은 하나도 없었습니다

사용자가 `/24` 를 의심했는데, 전부 훑어보니 **`/24`·`*24` 수식이 0건**이었습니다.
원인은 다른 것이었습니다 — `근퇴계` D·E(출근·퇴근)가 근태원본에서 시각을
끌어오며 끝에 **`&""` 를 붙이고** 있었습니다.

```
=IFERROR(INDEX(근태원본!$D:$D, MATCH(…))&"", "")
```

`&""` 는 숫자를 **글자로** 바꿉니다. 엑셀에서 시각은 '하루의 몇 분의 몇'인
숫자(8:30 = 0.354166…)라, 글자가 되는 순간 **`h:mm` 서식이 아무 소용이 없습니다.**
빈 날에 `0` 이 뜨는 것을 막으려던 것으로 보입니다.

`&""` 를 떼고 **서식의 0 자리를 비워**(`h:mm;h:mm;`) 같은 효과를 냈습니다.
근무일명칭(C)·비고(K)도 `&""` 를 쓰지만 **그쪽은 원래 글자**라 그대로 뒀습니다.
**소수로 새는 곳은 D8:E38 62칸이 전부**입니다.

### 요일 색 — 칸이 아니라 글자색입니다

`근태원본`(A2:Y5953) · `근퇴계`(A8:K38) 두 곳. **근태가 없는 날도 칠합니다**
(사용자가 '달력식'을 골랐습니다).

**칸을 칠하지 않은 이유** — 칸을 칠하면 이미 있는 **사람 블록 줄무늬**와
**노란 입력칸**을 덮어 버려서 넣을 자리를 못 찾게 됩니다.

**토요일 공휴일은 대체공휴일이 있느냐로 갈립니다.**
처음에는 "토요일이 공휴일이면 무조건 빨강"으로 넣었는데, 사용자가
**"대체공휴일이 있으면 파랑, 없으면 빨강"** 으로 고쳐 주었습니다.
대체가 생기면 **쉬는 날이 뒤로 넘어가니** 그 토요일은 토요일 취급이고,
안 생기면 **그날이 곧 공휴일**이라는 뜻입니다.

| 2026년 토요일 공휴일 | 대체 | 색 |
|---|---|---|
| 8/15 광복절 | 8/17 | 파랑 |
| 10/3 개천절 | 10/5 | 파랑 |
| 6/6 현충일 | 없음 | 빨강 |
| 9/26 추석연휴 | 없음 | 빨강 |

**어느 공휴일에 대체가 붙는지 박아 넣지 않았습니다.** `월설정` G·H 에서
`COUNTIFS(공휴일날짜,">"&$B2,공휴일날짜,"<="&$B2+3,공휴일이름,"대체공휴일")>0`
으로 **그날 뒤 사흘 안**을 셉니다. 해가 바뀌어도 표만 채우면 따라갑니다.

24개월 730일을 돌려 **두 색이 동시에 켜지는 날 0개**를 확인했습니다
(파랑 102일 · 빨강 121일).

> 도구를 두 번 돌려도 규칙이 쌓이지 않게, 넣기 전에 **글자색이 빨강·파랑인
> 기존 규칙을 걷어냅니다.** 규칙이 4개/2개로 유지되는 것을 확인했습니다.

### ★ 색만 파랗고 구분은 '특근' 이었습니다 — 급여가 걸린 건

색을 넣고 나서 사용자가 찾아냈습니다. **"광복절이 색만 파랗고 휴일 근무로
잡혀 있다"** — `U` 열(구분)의 토요 판정이 `$C2="휴일"` 만 보고 있어서,
**이름이 붙은 날은 토요일이어도 무조건 '특근'** 으로 갔습니다.

색 규칙만 고치고 **구분을 같이 안 고쳐서 생긴 어긋남**입니다.
이제 **둘이 같은 식(`sub_expr()`)을 나눠 씁니다.**

```
전  IF(AND($R2="토",$C2="휴일"),"토요","특근")
후  IF(AND($R2="토",OR($C2="휴일",대체있음)),"토요","특근")
```

**이건 보기가 아니라 돈입니다.**

| | 계산 |
|---|---|
| 휴일특근수당 (AB) | 통상시급 × 1.5 × **실적시간**(근태집계 H) |
| 토요근무수당 (Z) | 통상시급 × 1.5 × **고정시간**(직원설정·월별입력) |
| 토요근무차감 (AC) | 실적이 고정에 못 미치면 그 부족분만큼 차감 |

8H 가 특근에서 토요로 옮겨가면 **특근수당 8H 어치가 빠지고**, 그만큼
**토요 실적이 채워져 차감이 줄어듭니다.** 차감이 이미 0이던 사람은
줄어들 차감이 없어 **그냥 빠지기만 합니다.** 사람마다 방향이 다릅니다.

24개월 통틀어 구분이 바뀌는 날은 **8/15 광복절 · 10/3 개천절 이틀**이고,
실제 자료로는 **8/15 에 네 사람**이 근무했습니다.
**그 네 사람의 2026-08 급여를 대장과 맞춰 봐야 합니다.**

`근태원본!U` 만 고치면 됩니다 — `근태집계` G·H 와 `근퇴계` H·I 가 모두
거기서 가져옵니다(`SUMIFS(…,U,"토요"/"특근")`). 다른 경로는 없습니다.

> **함정** — 조건부서식 수식 앞에 `=` 를 붙이면 안 됩니다. XML 은 `=` 없이
> 담아서, 붙이면 엑셀이 `==AND(…)` 로 읽어 **규칙이 조용히 죽습니다.**
> 처음 쓸 때 그렇게 나와서 고쳤습니다. 이미 있던 줄무늬 규칙도 `=` 가 없습니다.

### 근퇴계 가로 — 눕히기만 해서는 55%

세로로 길어서(62행) 가로 A4 한 장에 그냥 넣으면 **55%**, 11pt 가 6pt 가 됩니다.
사용자에게 둘을 물어 **급여산출을 오른쪽으로 옮기는 쪽**을 골랐습니다.

- `A~E 42~61행` → `M~P 7~26행` (80칸), 산출식 C:D 병합 18개는 풀어 O 한 칸에
- 날짜표 열 너비 142 → 115 (메모 K 는 22 로 넉넉히 — 비고가 깁니다)
- 가로 · A4 · 1쪽 × 1쪽 · 여백 0.25/0.4 · 인쇄범위 `A1:K62` → `A1:P40`
- **약 84%**

**행 높이는 안 옮겼습니다** — 급여산출 머리글이 30pt 라 그대로 가져오면
그 자리의 **날짜 줄 하나가 같이 두꺼워집니다.**

곁들여 둘을 옮겼습니다.
**`N1` → `T1`** (기본정보 몇 번째 줄을 볼지 정하는 숨은 칸. M~P 바로 위라
인쇄에 딸려 나옵니다. 이걸 보던 근퇴계 안 28칸도 함께 고침 — 밖에서 보는 곳 없음),
**`L3` → `R3`** (화면용 안내가 인쇄 범위에 들어가서).

### 강의안 변화

- 1일차 — 02장 색상 규칙 표 뒤에 **「글자색은 뜻이 다릅니다 — 달력과 같습니다」**
  안내 상자 신설. 05장 조회월 표의 근퇴계 줄에 가로 인쇄 한 줄.
  11장 마무리에 **근퇴계도 PDF로, 설정 건드리지 말 것** 한 줄.
- 2일차 — 10장 '한 달을 닫는 순서'에 근퇴계 가로 인쇄 한 줄.

### 확인한 것

- 백업과 한 칸씩 대조 — 색·시각 작업에서 **달라진 칸 62개(전부 근퇴계 D/E)**,
  가로 인쇄 작업에서 **근퇴계 밖 0개 · 옮긴 80칸 내용 차이 0개**
- `$N$1` 남은 칸 0개, 원래 자리 `A42:E61` 비었음
- XML 직접 확인 — `landscape · paperSize 9 · fitToWidth 1 · fitToHeight 1`,
  `scale` 속성 없음(배율과 '한 장에 맞추기'는 같이 못 씁니다)
- 강의 페이지를 다시 빌드하고 **암호를 넣어 복호화된 본문**에서 문구 확인

### 못 한 것

- **LibreOffice 가 없어 실제 계산·인쇄 미리보기를 못 봤습니다.**
  84% 는 열 너비·행 높이로 **셈한 값**입니다. 엑셀에서 `Ctrl+P` 로
  한 장에 들어오는지, **메모 칸이 잘리지 않는지** 눈으로 봐 주세요.
- 색도 규칙이 어느 날을 잡는지 **파이썬으로 모의**했을 뿐,
  화면에 실제로 빨강·파랑이 뜨는 것은 확인하지 못했습니다.

## 8월 금액 검증, 그리고 출퇴근이 글자였다는 것

### 구분을 바꿔도 금액은 사실상 그대로였습니다

사용자가 **"8월 대장이랑 맞춰보게 네 사람 금액 계산해줘"** 라고 해서,
워크북 수식을 파이썬으로 그대로 옮겨 2026-08 을 재현했습니다.

**네 사람 다 차인지급액 −10원, 한 사람은 0원. 합계 −30원.**

왜냐하면 넷 다 **토요 고정 32H 에 8H 모자란 24H** 였습니다.

```
전:  토요 32H(고정) − 8H(차감) + 특근 24H = 48H 어치
후:  토요 32H(고정) − 0H(차감) + 특근 16H = 48H 어치
```

**빠지는 특근수당과 없어지는 토요차감이 정확히 상쇄됩니다.**
남은 −10원은 `ROUNDDOWN(…,-1)` 의 10원 절사 잔차이고,
통상시급이 딱 떨어지는 사람은 잔차도 0입니다.
소득세는 구간이 안 바뀌고, 4대보험은 신고액 기준이라 둘 다 안 움직입니다.

| 사람 | 급여방식 | 차인지급액 변동 |
|---|---|---:|
| A | 일급제 | −10원 |
| B | 일수 | −10원 |
| C | 209H | 0원 |
| D | 209H | −10원 |

고정월급(자동차감 N)인 사람과 8/15 근무가 없던 사람은 변동 없습니다.

> **실명과 실제 금액은 이 문서에 적지 않습니다.** 이 저장소는 공개 배포되고,
> `.gitignore` 가 급여 자료를 커밋하지 않는 이유와 같습니다.
> 금액은 엑셀 파일에만 있습니다.

> **이것은 엑셀이 낸 값이 아니라 수식을 옮겨 재현한 값입니다.**
> LibreOffice 가 없어 실제 계산을 못 돌립니다. **재현값을 8월 대장과 먼저
> 맞춰 보고**, 맞으면 나머지를 믿으시면 됩니다.

### ★ 출퇴근이 시각이 아니라 글자였습니다

계산하다 발견했습니다. 파이썬이 `근태원본` D·E 를 읽으니 전부 `str` —
칸에는 `07:49` 로 보이지만 **"07:49" 라는 글자**였습니다.
근태기록 `.xls` 에서 복사해 온 그대로입니다. **706칸 전부**(예제 484칸).

**급여는 원래도 맞았습니다** — 엑셀이 뺄셈에서 글자를 숫자로 바꿔 주니까요.
그래서 `tools/text-to-time.py` 로 바꿔도 **급여는 한 원도 안 바뀝니다.**
바뀌는 것은 정렬·필터가 제대로 되고, `h:mm` 서식이 실제로 일을 하게 되는 것.

**앞선 판단 하나를 바로잡습니다.** 근퇴계 소수점(`0.354166…`)을 고칠 때
제가 "이것이 원인"이라고 단정했는데, 값이 글자이던 동안에는 **소수가 보이지
않았을 것**입니다. 정확히는 **그 경로가 원인이 될 수 있는 구조였다**가 맞고,
지금 시각 값으로 바꾸면서 **그 경로가 실제로 열립니다.**
`&""` 를 미리 떼 둔 덕에 지금은 `h:mm` 으로 제대로 나옵니다.

## 근퇴계가 안 맞던 진짜 이유 — 시급이 틀려 있었습니다

사용자가 회사에서 받은 8월 근퇴계의 시급·일급을 알려 주어 대조했습니다.
**두 사람이 어긋나 있었습니다.**

| 사람 | 급여방식 | 어긋난 내용 |
|---|---|---|
| A | 일급제 | 시급에 소수점이 붙어 **일급이 몇 원 어긋남** |
| B | 일수 | **시급 자체가 달랐음** (약 560원 낮게) |
| C · D | 209H | 받은 값과 이미 일치 |

**일급 = 시급 × 8** 이 정확히 맞는 값을 역산해 넣었습니다.
받은 근퇴계의 시급은 **화면에 반올림돼 보이는 값**입니다. 그 값을 그대로
넣으면 일급이 몇 원 틀어지므로, **일급 ÷ 8** 로 역산한 값을 넣었습니다.
자료를 못 받은 한 사람은 **손대지 않았습니다.**

### 통상일급만 혼자 10원 절사를 하고 있었습니다

```
전  일급제 → ROUND(시급×8, -1)
후  일급제 → 시급×8
```

209H·일수는 원래도 `시급×8`, 고정월급만 원 단위 반올림입니다.
**일급제만** 10원을 잘라 받은 근퇴계와 어긋났습니다.

### 급여 영향 — 한 사람이 큽니다

시급이 오른 B 는 **차인지급액이 십수만 원 늘어납니다**(소득세도 함께 오릅니다).
A 는 절사 잔차 수준. 나머지는 변동 없습니다.
**금액은 엑셀에서 확인하시고, 소급 정산이 필요합니다.**

### 기본정보에 아무도 안 보는 열이 셋

192행짜리 열이 **참조 0곳**으로 놀고 있었습니다.

| 열 | 무엇 | 왜 지웠나 |
|---|---|---|
| C | 주민번호 | 아무도 안 봄. **주민등록번호가 192줄 복제** |
| H | 비고 | 아무도 안 봄 |
| BA | 비고 | H 와 똑같은 수식. 완전 중복 |

머리글을 `(사용 안 함)` 으로 바꿔 비웠습니다. 원본은 [직원설정] 에 그대로 있습니다.
**A·B·D~G 는 그대로 뒀습니다** — 중복처럼 보이지만 전체임금대장·급여명세서가
실제로 가져다 쓰는 조회표입니다.

### 곁들여 둘

- **근퇴계 5·6행에 통상시급·통상일급**을 띄웠습니다. 받은 근퇴계와
  대조할 값이 화면에 없어서, 숫자가 어긋나면 **여기부터 보게** 했습니다.
- **근태원본 이름(A열)을 요일 색에서 뺐습니다.** 조건부서식 범위를
  `A2:Y5953` → `B2:Y5953` 으로. 줄무늬는 A열까지 그대로입니다.

## 새로 만든 것 — 마스터 클래스 (`excel-m1` ~ `excel-m9`)

지금까지의 실무 편(`excel-4`·`excel-5`)이 **우리 회사 파일 하나**를 깊게 판다면,
이쪽은 **엑셀이라는 도구 자체**를 넓게 훑습니다.
오빠두 『진짜 쓰는 실무 엑셀』 목차를 따라가는 교안이고,
**개인 교육용이라 암호 없이 공개**합니다.

### 아홉 쪽 (104강, 960분)

| 쪽 | 제목 | 강 → 절 | 분 |
|---|---|---|---|
| `excel-m1` | 처음에 해 두면 평생 편한 것들 | 12 → 10 | 120 |
| `excel-m2` | 남이 만든 엉망인 표를 쓸 수 있게 | 13 → 11 | 110 |
| `excel-m3` | 같은 표인데 읽히는 표 | 7 → 8 | 80 |
| `excel-m4` | 남에게 넘기기 전에 | 15 → 9 | 130 |
| `excel-m5` | 수천 줄에서 필요한 것만 | 12 → 8 | 100 |
| `excel-m6` | 자료가 늘어도 보고서는 그대로 | 15 → 8 | 120 |
| `excel-m7` | 열 몇 개면 거의 다 됩니다 | 22 → 9 | 170 |
| `excel-m8` | 그림이 거짓말하지 않게 | 5 → 7 | 80 |
| `excel-m9` | 한 걸음 더 (부록) | 3 → 5 | 50 |

**강의 하나에 한 절을 주면 목차가 스물다섯 줄이 되어 못 읽습니다.**
그래서 주제별로 묶었습니다. 기존 강의안과 **같은 틀**입니다 —
`.shell.with-rail` · `00 — 준비` 장 · 끝의 확인 목록.
`excel.html` 「엑셀을 제대로 다시」 절에 카드 아홉 장이 있습니다.

### 장마다 「제일 큰 사고」를 하나씩 `.note stop` 으로

| 장 | 사고 |
|---|---|
| 3장 | 단위를 손으로 치면 그 줄이 합계에서 빠집니다 |
| 4장 | 수식이 보는 것을 함께 안 옮기면 받는 쪽에서 `#REF!` |
| 5장 | 소계가 섞인 표는 합계가 **정확히 두 배** |
| 6장 | 가로로 퍼진 자료로는 「연도」라는 필드가 안 생깁니다 |
| 7장 | `VLOOKUP` 네 번째 칸을 비우면 **다른 제품 값**이 들어옵니다 |
| 8장 | 실적과 예상을 같은 선으로 그리면 거짓말이 됩니다 |

**여섯 개 전부 오류가 안 뜹니다.** 그게 공통점이고, 이 편의 축입니다.
고칠 때 이 축을 흐리지 마세요.

### 예제 223개 — 강의 번호로 다시 깔았습니다

원본은 책 기준 번호(`03-001` · `07-A02` · `A-003`)라
**어느 강의 것인지 알 수가 없습니다.** 그래서 파일을 열어
**시트 이름과 원본↔완성 차이**를 보고 번호를 붙였습니다.

```
python tools/build-lesson-examples.py            # 보기만
python tools/build-lesson-examples.py --write    # 실제로 깔기
```

매핑 근거의 예 — 완성본에 `dataBar` 규칙만 생기면 **3-5 데이터 막대**,
`lockStructure="1"` 이 붙으면 **4-10 통합 문서 보호**,
`=SUM` 이 `=SUBTOTAL` 로 바뀌면 **5-8 SUBTOTAL**.
**`CHAPTERS` 만 고치고 다시 돌리면 됩니다.** 3~8장에는 이제 (추정)이 없습니다.

### ★ 2026-09-18 — 열네 강의 이름이 틀려 있었습니다

처음 매핑은 **셀 값과 서식만** 견줬습니다. 그래서 완성본에서
**글자색 · 시트 보호 · 문서 속성(만든 이) · 피벗 설정(`showDataAs`·값 필터)**
만 바뀐 파일을 「바뀐 게 없다」 고 보고 **짐작으로 채웠습니다.**

다시 볼 때는 두 가지를 맞췄습니다.

1. **책의 절 단위 목차** — [예스24](https://www.yes24.com/Product/Goods/106711883) 상품 페이지에
   「[실무 활용] 입력하는 값 & 계산되는 값 구분하기」 같은 줄까지 있습니다.
2. **원본↔완성 XML 차이** — `showGridLines` · `sheetProtection` · `dc:creator` ·
   `showDataAs` · `<top10>` · 슬라이서 `style` 같은 것들.

| 강 | 붙였던 이름 | 완성본에서 실제로 바뀐 것 | 고친 이름 |
|---|---|---|---|
| 4-02 | 인쇄 영역과 눈금선 | 눈금선·**행열 머리글** 끔, 페이지 레이아웃 보기 | 눈금선과 머리글 끄기 |
| 4-03 | 시트 복사와 수식 | 입력 칸 **파란 글자** (`0070C0`) | 입력값과 계산값 구분 |
| 4-05 | 공공데이터 받아 쓰기 | (완성 없음) 메타데이터 시트 | 자료 출처 명시 |
| 4-06 | 열 순서 바꾸기 | 만든 이 「[인사팀] 박상혁」 **삭제** | 개인 정보 제거 |
| 4-09 | 그림 다루기 | **시트 보호** + 필터 허용 | 시트 보호 |
| 4-13 | 긴 표 인쇄 | **머리글·바닥글** (`&P/&N`, [대외비]) | 페이지 번호 |
| 4-14 | 머리글과 바닥글 | 머리글 그림 | 워터마크 (이름만 책에 맞춤) |
| 5-02 | 여러 시트 취합 | (완성 없음) 같은 모양 12장 | 여러 시트 동시 편집 |
| 5-05 | 한 칸에 뭉친 값 | (완성 없음) `지사-이름(직급)` | 와일드카드 필터 |
| 5-07 | 숫자 필터와 색 필터 | `>=0.1` + **상위 10** | 이익률 필터와 TOP10 |
| 6-02 | 표의 구조적 참조 | (완성 없음) 병합·두 줄 머리글·같은 열 이름 | 표로 바꿀 때 주의할 점 |
| 6-06 | 피벗 새로 고침 | `showDataAs` 누계·비율·차이 | 값 표시 형식 |
| 6-09 | 보고서 레이아웃과 부분합 | 값 필터 **상위 3** + 내림차순 | 우수 고객 필터 |
| 6-14 | 피벗 차트 | 차트 없음, 슬라이서 **스타일 「내슬라이서」** | 슬라이서 꾸미기 |

**믿어도 되는 근거** — 고친 이름으로 다시 놓으면 **4·6장 파일 번호가 책의 절 순서와
하나도 어긋나지 않습니다.**

**함께 고친 것**
- 파일 26개를 `git mv` 로 옮겼습니다(이력이 남습니다).
- `excel-m4` 는 **책의 여덟 절 순서대로 다시 조립**했습니다. 01 100점짜리 보고서 ·
  02 외부 참조 · 03 출처·개인 정보·버전 · 04 크기 · 05 잠그기 · 06 인쇄 · 07 여러 장.
  「열 순서 바꾸기」(`Shift`+끌기)와 「그림 다루기」 절은 **예제가 없어** 뺐고,
  그림 압축 이야기만 04절 쪽지로 남겼습니다.
- `excel-m5` — 5-02 는 시트 그룹 편집으로, 5-05 는 정렬 절에서 **필터 절로 옮겨** 와일드카드로,
  5-07 은 이익률 10% + 상위 10 으로. 「색 기준 필터」는 예제가 없어 뺐습니다.
- `excel-m6` — 6-02 주의할 점 절 신설, **값 표시 형식**을 03절 끝에, **우수 고객 필터**를
  04절에, 06절 「피벗 차트」 를 **슬라이서 꾸미기**로. 새로 고침 설명은 예제 없이 남겼습니다.
- 4장 복습의 「4-01 파일 이름」 시트 → 「파일 이름」, 4장 종합문제 ⑥ 강의 번호 4-02 → 4-11.

**다시 하지 마세요** — 셀 값만 견주는 비교 도구로 매핑을 정하지 마세요.
완성본에서 바뀐 게 없어 보이면 **XML 을 열어 보는 것**이 먼저입니다.

- **2026-09-18 부터 셋 다 저장소에 들어 있습니다** — 사용자가 `.gitignore` 에서 뺐습니다.
  · 원본 폴더 `진짜쓰는 실무엑셀 예제파일/` (75MB)
  · **`4-07 파일 크기 줄이기` 네 파일** — 25MB·19MB·12MB·7MB.
    **크기를 견주는 것이 강의 내용**이라 줄일 수가 없습니다
  · `강의예제/*.zip` (1·2장 묶음)
- 가장 큰 파일이 **24.8MB** 로 GitHub 한도(50MB 경고 · 100MB 거부) 안쪽입니다.
  **50MB 가 넘는 파일을 새로 넣게 되면** 그때는 막아야 합니다.
- 전에는 `.gitignore` 로 막아 두었고 `--write` 로 로컬에만 깔았습니다.
  그 무렵 문서가 「저장소에 없다」고 적혀 있었다면 그 때문입니다.

### 복습 여덟 개 · 종합문제 여덟 개

```
python tools/build-review-book.py --write     # N장_복습.xlsx
python tools/build-final-exam.py --write      # N장_종합문제.xlsx
```

복습은 **기술을 하나씩**(시트마다 한 강의), 종합문제는 **엉망인 표 한 장**을 받아
그 장에서 배운 것을 **전부 동원**합니다.

**종합문제 답은 하드코딩이 아니라 도구가 자료에서 뽑습니다.**
`C5_DATA` 같은 자료를 고치면 정답과 「틀렸다면」 설명이 함께 따라갑니다.

| | 상황 | 최종확인 다섯 칸 |
|---|---|---|
| 3장 | 한빛물산 상반기 영업보고 | 622,350,000 / 4 / 강남점 / 4 / (단위: 천원) |
| 4장 | 대림기업 3월 견적서 | 270 / 183,150 / 2 / 480 / `$5:$5` |
| 5장 | 세아물산 상반기 주문내역 | 11 / 21,580,000 / 5,180,000 / 6 / 9,320,000 |
| 6장 | 세나상사 지점별 매출 | 25 / 110,000 / 서초점 / 6.47 / **194.59** |
| 7장 | 한결상사 거래처 정산표 | 4 / 2,587,500 / 한결상사 / 3 / 7 |
| 8장 | 거짓말하는 차트 네 개 | 0 / 7.3 / 항목을 거꾸로 / 4 / 점선 |

**6장 ⑤가 이 편에서 제일 잘 나온 문제입니다.** 원본에 이익률 열을 만들어 피벗에서
「합계」로 내면 총합계가 **6.47%가 아니라 194.59%** 로 나옵니다.
줄별 비율 스물다섯 개를 그냥 더한 값인데, **피벗은 오류 없이 내놓습니다.**
(처음에는 「계산 항목으로 내면」 이라고 적었는데, 엑셀에서 돌려 보니 계산 항목은
**한 필드 안의 항목끼리만** 계산할 수 있어 `= 이익 / 매출` 을 **거부합니다.** 아래 절을 보세요.)

> **8장 종합문제만 노란 칸이 0개입니다.** 채우는 문제가 아니라
> **고칠 곳을 찾는 문제**라 그렇습니다. 버그가 아닙니다.

### 장 제목의 출처

**1·2장은 마스터 클래스 강의 목차**, **3~8장과 부록은 책의 챕터 제목**입니다.
원본 폴더에 목차 파일이 없어서 [예스24 상품 페이지](https://www.yes24.com/Product/Goods/106711883)에서
확인했습니다. 폴더 이름도 그 제목을 따릅니다.

### 새 도구 — `tools/check-pages.py`

```
python tools/check-pages.py            # excel-m*.html 전부
```

강의안에서 **차례와 본문이 어긋난 곳**을 찾습니다 — 네 가지를 봅니다.
차례의 `#앵커` 가 실제 `section id` 와 순서까지 맞는가,
`href="…html"` 이 있는 파일을 가리키는가,
`<code>` 안의 **강의 번호**가 `강의예제/` 에 있는가, 태그 짝이 맞는가.

**본문에서는 `1-03_기본기_1` 처럼 줄여 쓰므로 번호만 봅니다.**
전체 이름을 맞춰 보게 하면 멀쩡한 곳이 잔뜩 걸립니다(한 번 겪었습니다).

### 확인한 것

- 로컬 서버로 **아홉 쪽 전부** — 차례 = 섹션, 깨진 앵커 0, 콘솔 오류 0.
- **가로 넘침 0** (`main` 밖으로 나가는 요소 없음).
- `excel.html` 카드 14장이 전부 같은 크기로 섭니다.
- 확인 목록 저장 키가 쪽마다 분리됩니다 (`sumgo-check:excel-m3.html:0`).
- 3장 목업 그림이 `viewBox` 안에 들어옵니다 (700×163 / 700×208).
- 워크북 16개 — 노란 칸 22~100개, 주황 칸 5개, `[정답]` 시트 전부 있음.

### 겪은 일 둘 (다시 만나면 바로 알아보시라고)

- **`--write` 가 1장 예제 네 개를 덮어썼습니다.** 커밋된 것과 바이트가 달라서
  `git checkout` 으로 되돌렸습니다. 사용자가 열어서 저장한 적이 있는 듯합니다.
  **`--write` 전에 `git status` 를 한 번 보세요.**
- **`1장_복습.xlsx` 은 되돌리지 않았습니다.** 다시 만드니
  `1-11` 시트의 부제 한 줄이 되살아났는데, 예전 판 도구로 만들어
  빠져 있던 칸이었습니다. 지금 도구가 내는 것이 맞습니다.

## 남은 일

- [ ] **전체 검토받기** — 강의안 28개의 분량·깊이·말투.
      아직 사용자 확인을 못 받았습니다.
- [x] ~~**마스터 클래스 (추정) 일곱 곳 확인**~~ — 일곱 중 넷이 틀렸고,
      (추정)을 안 붙인 곳에서도 다섯이 더 틀려 **열네 강의 이름**을 고쳤습니다.
      위 「★ 2026-09-18 — 열네 강의 이름이 틀려 있었습니다」 절을 보세요.
- [x] ~~**1·2장 (추정) 두 쌍**~~ — 확인했습니다.
      · **`2-07`/`2-10` 은 틀려 있었습니다.** 같은 파일 `02-009` 를 나눠 쓰고, 정작
        숫자 단위 파일 `02-006` 은 `2-02 그룹 기능_1` 로 가 있었습니다.
        `02-006` 완성본은 `#,##0.0,,` 표시 형식 + **나누기 붙여넣기**(G16 에 천만) → **2-07**,
        `02-009` 완성본은 **이름 정의 20개** + `=INDIRECT(M8) INDIRECT(M9)` 교집합 → **2-10**,
        `02-A02` 는 열 윤곽 **1·2단계** → **2-02** 하나만. 책 2장 목차에서도
        「숫자 단위」(84쪽)와 「이름 정의」(93쪽)는 **다른 절**입니다.
        `excel-m2` 의 2-02 · 2-07 · 2-10 「해 보기」 를 파일대로 다시 썼습니다.
        특히 2-07 은 전에 「실제로 나누면 안 된다」 고만 적었는데, 책과 예제는
        **나누어 붙여넣기와 표시 형식 두 방법**을 다 가르칩니다.
      · **`1-02`/`1-12` 는 그대로 둡니다.** `01-A08` 열두 시트는 단축키 연습 묶음이라
        `1-12` 는 확실하고, 빠른 실행 도구모음만 다루는 파일은 원본 1장에 **없습니다**
        (쓰지 않은 일곱 개도 다 열어 봤습니다). 그래서 `1-02` 가 첫 시트를 빌려 쓰는 것이
        유일한 선택이고, 이것만 (추정) 으로 남깁니다.
- [x] ~~**마스터 클래스 목업 그림**~~ — 5장 「소계가 섞인 표」(2,144 → 1,072),
      6장 「가로로 퍼진 표를 눕히기」, 7장 `INDEX`/`MATCH` 동작,
      8장 「축이 250부터」(8.8배 → 2.3배) 넷을 넣었습니다. 1~8장에 모두 열네 개.
      8장 예시를 「600부터」에서 **「250부터」**로 바꿨습니다 — 600부터면
      경기(301)가 축 아래로 가라앉아 **막대가 아예 안 보여서**, 말하려던
      「차이가 부풀려진다」가 그림으로 안 됐습니다. 종합문제도 같은 이유로 **50부터**.
- [x] ~~**종합문제를 실제로 풀어 보기**~~ — 2026-09-18, **진짜 엑셀(COM)로** 풀었습니다.
      `python tools/check-final-exam.py` — 1~7장 35문항 **전부 정답과 맞습니다.**
      숫자는 처음부터 맞았는데, **엑셀에서 해 보니 안 되는 것**이 여섯 나왔습니다.
      · **5·7장 파일이 엑셀에서 아예 안 열렸습니다.** 정답 설명이 `=LEN(B6). 「한결…` 처럼
        `=` 로 시작해 openpyxl 이 **수식으로** 저장했습니다. 두 빌더의 `put()` 에서
        `=` 로 시작하는 글자는 글자로 저장하게 고쳤습니다(복습 7-05 에도 하나 있었음).
      · **3장** 정답 ⑤~⑦ 이 **한 열씩 밀린 주소**였습니다. 문구대로 ⑤ `J6=I6/H6` 을 하면
        상반기 합계 159,250,000 이 6.29 로 **덮어써집니다.** → `K6=J6/I6`, `=$K6<1`, J열 막대 · L열 스파크라인.
      · **4장** ⑤ 에 `IFERROR` 가 빠져 금액 합계가 `#N/A`. 「확인한 뒤 맨 마지막에 감싼다」 를 넣었습니다.
      · **6장** ⑤ 「계산 항목으로 이익률」 은 엑셀이 거부합니다 → 「원본 이익률 열을 합계로」 로.
        같은 김에 `excel-m6` 의 「계산항목오류」 설명도 파일대로 고쳤습니다 — 그 시트의 함정은
        비율이 아니라 **「20-21년 차이」 줄이 총합계에 더해져 83,000 이 76,000 이 되는 것**입니다.
        6장 복습 6-11 에 이것을 묻는 ④ 를 더했습니다.
      · **7장** 단가표가 제품코드→단가 순이라 **VLOOKUP 이 그냥 됐습니다**(함정 불성립).
        단가→제품코드 순으로 바꿔 이제 `#N/A`. 「경과 개월」 칸이 없어 J열을 새로 두고,
        기준일(F3)을 글자에서 진짜 날짜로.
      · **8장** 은 「그려진 차트를 고친다」 는데 **차트가 하나도 없었습니다.** 증상대로 그린
        차트 다섯(축 50부터 · 순서 반대 · 예상치 실선 · 조각 9개 · 빨강/초록)을 H열에 넣었습니다.
- [ ] **줄 길이 확인** — 본문이 칸을 꽉 채웁니다.
      강의안(1180px)은 보통 **65자**, 첫 화면·과목 안내(1524px)는 **84자**입니다.
      한글 통상 범위(35~45자)를 꽤 넘습니다.
      **사용자가 "여백 없이 꽉 채워 달라"고 세 번 요청해서 이렇게 한 것입니다.**
      읽기 어렵다고 하시면 `--measure`에 `1000px` 정도를 주세요.
      다만 그러면 글 오른쪽에 빈 띠가 다시 생기고, 오른쪽 끝 정렬도 깨집니다.
      **둘 중 하나는 포기해야 합니다.**
- [ ] **시간 배분 재검토** — 부당 80~110분으로 잡았으나(지금은 80~130분)
      실제로 한 번 진행해 보고 정할 일입니다.
      **2026-09-18 분량으로 재 본 것**(실제 진행 전이라 참고만):
      - 마스터 클래스는 「읽기 250자/분 + 실습 파일 하나 5분(차트 8분)」으로 셈하면
        1·2·5·6장은 지금 값과 ±10분 안입니다.
        **7장**(22강·실습 24개, 150분 → 셈 165분)과 **4장**(15강·실습 19개, 110분 → 셈 136분)이 빠듯하고,
        **3장**(7강, 90분 → 셈 70분)·**8장**·**부록**은 넉넉합니다.
      - 기초 과목은 「읽기 150자/분 + 단계 하나 1.5분 + 절 하나 2분」으로 셈하면
        **3부가 모자랍니다** — `excel-3` 110→셈 134, `hangul-3` 90→107, `word-3` 90→106.
        1부는 셈보다 10분쯤 넉넉한데, 처음 켜는 분이라 그대로 두는 것이 맞아 보입니다.
      - AI 다섯 부는 단계 목록이 거의 없고 AI 답을 기다리는 시간이 커서 이 셈이 안 맞습니다.
      - **복습·종합문제 시간은 「소요」에 안 들어갑니다**(쪽마다 「며칠 뒤에」 하라고 되어 있음).
      - **위 여섯 곳은 같은 날 바꿨습니다** — 마스터 7장 170 · 4장 130 · 3장 80,
        `excel-3` 130 · `hangul-3` 100 · `word-3` 100. 마스터 합계 930 → 960분.
        실제로 한 번 진행해 본 뒤 다시 맞추세요.
      - 값을 바꾸면 **쪽의 `소요` · 과목 안내 카드 badge · 이 문서의 표**를 같이 고치세요.
        카드와 쪽 값이 같은지는 2026-09-18 에 전부 맞춰 봤습니다(어긋난 곳 0).
- [x] ~~**git 저장소로 만들기**~~ — 저장소가 되었고 첫 커밋이 있습니다.
- [x] ~~**Vercel 배포**~~ — <https://sumgo6615.vercel.app> 에 올라가 있습니다.
      GitHub `inchoriya1/sumgo6615`(**공개 저장소**) 에 푸시하면 자동으로 배포됩니다.
      **푸시 전에 `엑셀자료/` 와 `*.src.html` 이 커밋에 안 들어갔는지 꼭 확인하세요.**
      `git diff --cached --name-only` 로 한 번 훑으면 됩니다.
- [x] ~~**실무 편 2일차**~~ — `excel-5.html` 로 만들었습니다. 위 절을 보세요.
- [ ] **실무 편을 실력 진단에 넣을지** — `check.html` 에 아직 문항이 없습니다.
      넣으려면 `check.js` 의 `PARTS` 표에 `excel-4` · `excel-5` 를 먼저
      추가해야 합니다(안 그러면 결과에서 조용히 빠집니다).
      다만 실무 편은 **암호가 걸린 사내용**이라, 공개 진단에서 권하는 것이
      맞는지부터 정해야 합니다.
- [ ] **간이세액표 최신판으로 교체** — 사용자에게 국세청 엑셀 파일을 부탁했습니다.
      받으면 `tools/swap-tax-table.py` 로 한 번에 바꿉니다. 위 절을 보세요.
- [ ] **1천만원 초과 구간 산식** — 경고와 직접입력 칸으로 막아 두었을 뿐,
      **공식 산식은 아직 안 들어갔습니다.** 국세청 자료를 구하면 넣을 수 있습니다.
- [ ] **보완본을 실제로 쓸지 결정** — `…_상한초과보완.xlsx` 를 확인하고 갈아탈지.
- [x] ~~**2026년 공휴일 표 채우기**~~ — 스물한 개를 다 넣었습니다(`tools/fill-holidays.py`).
- [ ] **2027년 공휴일 표** — 설날·부처님오신날·추석은 음력이라 날짜가 달라지고
      대체공휴일도 요일에 따라 바뀝니다. 해가 바뀌기 전에 넣으세요.
      **제헌절(7/17)은 2026년부터 법정공휴일**이라 해마다 들어갑니다
      (토·일과 겹치면 대체공휴일도 함께).
- [ ] **급여대장 파일 자체의 개선 제안** — 요율(4.75% 등)이 수식 안에
      박혀 있습니다. 설정 시트의 칸을 가리키게 바꾸면 해마다 칸 하나만
      고치면 됩니다. 강의안 11장에 이 이야기를 적어는 두었습니다.
- [ ] **표의 가로 스크롤 표시** — 좁은 화면에서 표와 그림이
      `.tbl-wrap`(`overflow-x:auto`) 안에서 잘립니다. 밀면 보이기는 하는데
      **밀 수 있다는 표시가 없습니다.** 사용자가 "모바일은 안 본다"고 하여 손대지 않았습니다.
- [ ] **복사 단추 실제 확인** — `.say-copy` 를 브라우저 자동화로 눌렀더니
      "복사가 안 됩니다"가 떴습니다. **사람이 직접 누른 것이 아니라
      클립보드 권한이 막힌 것**으로 보입니다(이전 세션에서는 실제 클릭으로 동작 확인).
      한 번 손으로 눌러서 확인해 주세요.
- [ ] **진단 결과 PNG 실제 확인** — 코드 경로와 예외 처리는 확인했지만
      내려받은 그림을 눈으로 보지는 못했습니다(샌드박스가 내려받기를 막습니다).

## 확인해 둔 것

- **모든 페이지의 내부 링크·앵커 검사 통과** (18개 파일, 깨진 것 0개).
- 강의안 12개 전부 **장 수 = 목차 항목 수**, `00 — 준비` 장 있음,
  `03장` 실습에 무료 대안 안내 있음, `data-subject` 올바름.
- 화면 폭 1905·1425·1265·885와 글자 크기 세 단계에서
  **가로 넘침 0개**, 콘솔 오류 없음. 밝은 화면·어두운 화면 모두.
- 첫 화면·과목 안내 네 곳, 강의안 12곳, `ai.html` 까지 전부 확인했습니다.
- 확인 목록 저장이 **부마다 분리**되는 것을 클릭으로 확인했습니다
  (`sumgo-check:excel-1.html:2` 처럼 파일 이름이 키에 들어갑니다).
- `.say`(지시문 카드)는 이제 **`ai.html`에서만** 쓰입니다.
  `README.md`의 `.say` 설명도 실제 동작에 맞게 고쳤습니다
  (단추는 마크업에 직접 넣어야 하고 자동으로 생기지 않습니다).

## 이 세션에서 쓴 것

로컬 확인용으로 `.claude/launch.json` 을 만들었습니다
(`python -m http.server 8123`). `file://` 로 열면 CSS가 안 붙어서
스타일 확인이 안 됩니다. 반드시 서버로 띄워서 보세요.
